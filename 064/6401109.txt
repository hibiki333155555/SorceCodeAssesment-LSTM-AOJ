#include<stdio.h>
int main() {
    int i, sum;
    char num[1001];
    for (;;)  {
        if ( num[0] =='0') break;
        sum = 0;
        for ( i = 0; i != '\0'; i++ ) {
            sum += num[i] - '0';
            print("%d\n", sum);
        }
        return 0;
    }
}
