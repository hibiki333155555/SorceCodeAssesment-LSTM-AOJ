#include <bits/stdc++.h>
using namespace std;

int main() {
  int s;
  double w, h;
  char c;
  while (cin >> s >> c >> w >> c >> h) {
    if (w / h / h >= 25) {
      cout << s << endl;
    }
  }
}

