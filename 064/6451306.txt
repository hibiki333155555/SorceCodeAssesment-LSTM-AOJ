#include<bits/stdc++.h>
using namespace std;
int main()
{
    int q;
    

    cin>>q;

    set <int> s;


   while(q--)
    {

            int a,x;
            cin>>a;
            if(a==0)
            {
                cin>>x;
                
                s.insert(x);

                cout<<s.size()<<endl;

            }
            if(a==1)
            {

               set< int > :: iterator it;
           int c;
          cin>>c;
          it=s.find(c);
          if(it!=s.end())
          {
              cout<<"1"<<endl;
          }
         
         else
          {
              cout<<"0"<<endl;
          }

            }

            if(a==2)
            {
                cin>>x;
                s.erase(x);
               
            }

            if(a==3)
        {
            int l,r;
            cin>>l>>r;
            for(auto it=s.begin();it!=s.end();it++)
            {
                if(*it>=l && *it<=r)
                cout<<*it<<endl;

            }
        }
               
               


 }
        

        return 0;


}
