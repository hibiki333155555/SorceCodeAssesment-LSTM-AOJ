#include <iostream>
// #include "comp.h"
using namespace std;

int main()
{
    int x;
    cin >> x;
    cout << (x / 3600) << ":";
    x = x - (x / 3600) * 3600;
    cout << (x / 60) << ":";
    x = x - (x / 60) * 60;
    cout << x << endl;
}

