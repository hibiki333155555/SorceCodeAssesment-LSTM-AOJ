#include<stdio.h>
#include<math.h>

int main()
{
	double r, area, circumference;
	double pi = acos(-1);
	scanf("%lf", &r);
	area = pi*pow(r,2);
	circumference = 2*pi*r;
	printf("%lf %lf", area, circumference);
	return 0;
}
