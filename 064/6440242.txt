#include <stdio.h>

int stack[45];

int fib(int n){
	if(n == 0 || n == 1){
		return 1;
	}
	if(stack[n] == 0){
		stack[n-1] = fib(n-1);
		stack[n-2] = fib(n-2);
		stack[n] = stack[n-1] + stack[n-2];
	}
	return stack[n];
}

int main(){
	int n;
	scanf("%d", &n);

	printf("%d\n", fib(n));
}

