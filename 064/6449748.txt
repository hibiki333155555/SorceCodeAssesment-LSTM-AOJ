#include <stdio.h>
#include <bits/stdc++.h>
using namespace std;
#define rep(i,n) for(int i=0;i<(n);i++)
#define Inf 1000000001

int main(){
	
	int a,b;
	cin>>a>>b;
	int c = max(a,b);
	int t = 0;
	rep(i,30){
		if((c>>i)&1)t = i;
	}
	int ans = 0;
	for(int i=t;i>=0;i--){
		if(((a>>i)&1) == ((b>>i)&1))ans++;
		else break;
	}
	cout<<ans<<endl;
	
	return 0;
}
