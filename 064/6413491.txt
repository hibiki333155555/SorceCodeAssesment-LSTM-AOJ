#include <bits/stdc++.h>

using namespace std;

int euclidean_algorithm(int x, int y) {
  int mod = x % y;
  if (mod == 0) {
    return y;
  }
  return euclidean_algorithm(y, mod);
}

int main() {
  int x, y;
  scanf("%d", &x);
  scanf("%d", &y);

  cout << euclidean_algorithm(x, y) << endl;
}
