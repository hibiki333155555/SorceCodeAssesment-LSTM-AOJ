const x = require('fs').readFileSync('/dev/stdin', 'utf8');
const arr = x.split(' ').map(Number);
const d = Math.floor(arr[0] / arr[1]);
const r = arr[0] % arr[1];
const f = (arr[0] / arr[1]).toFixed(5);
console.log(`${d} ${r} ${f}`);
