#include<bits/stdc++.h>
using namespace std;
#define rep(i,n) for(int i=0;i<(n);i++)
//using ll=long long;
int main(){
    vector<int> fib(2,1);
int now_i;
while(fib[now_i]<1e8){
    now_i++;
    int k=fib[now_i-1]+fib[now_i-2];
    fib.push_back(k);
}
int row_i=1;
    int x,y;cin>>x>>y;
    int rl,ud;
    rl=((x>0)?rl=1:rl=3);
    ud=((y>0)?ud=2:rl=0);
    int xk=0,yk=0,ans=1,i=1;
    while(xk<abs(x)||yk<abs(y)){
        if(i%4==rl)xk+=fib[i];
        if(i%4==ud)yk+=fib[i];
        ans++;
        if(ans==4)ans=1;
        i++;
    }
    cout<<ans<<"\n";
   /* int ap=0,bp=0,c=1,am=0,bm=0,a=1,b=1;
    /*while(true){
    for(int i=0;i<100;i++){
        if(3<c)c=1;
        if(am<=x&&x<=ap&&bm<=y&&y<=bp){
            cout<<c<<"\n";
            return 0;
        }
        a+=b;
        ap+=b;
        c++;
     //   cout<<a<<" "<<b<<" "<<ap<<" "<<bp<<" "<<am<<" "<<bm<<"\n";//2,1
        if(3<c)c=1;
        if(am<=x&&x<=ap&&bm<=y&&y<=bp){
            cout<<c<<"\n";
            return 0;
        }

        b+=a;
        bp+=a;
        c++;
        //cout<<a<<" "<<b<<" "<<ap<<" "<<bp<<" "<<am<<" "<<bm<<"\n";//2,3
        if(3<c)c=1;
        if(am<=x&&x<=ap&&bm<=y&&y<=bp){
            cout<<c<<"\n";
            return 0;
        }
        a+=b;
        am+=-a;
        c++;
       // cout<<a<<" "<<b<<" "<<ap<<" "<<bp<<" "<<am<<" "<<bm<<"\n";//5,3
        if(3<c)c=1;
        if(am<=x&&x<=ap&&bm<=y&&y<=bp){
            cout<<c<<"\n";
            return 0;

        }
        b+=a;
        bm+=-a;
        c++;
        
        //cout<<a<<" "<<b<<" "<<ap<<" "<<bp<<" "<<am<<" "<<bm<<"\n";//5,8
        if(3<c)c=1;
        if(am<=x&&x<=ap&&bm<=y&&y<=bp){
            cout<<c<<"\n";
            return 0;
        }
    }*/
        
    //}
    return 0;
}
