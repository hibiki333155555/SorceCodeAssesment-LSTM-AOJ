#include <algorithm>
#include <cstdio>
#include <cstring>
#include <deque>
#include <iostream>
#include <map>
#include <queue>
#include <set>
#include <stack>
#include <string>
#include <vector>
using namespace std;
using ll = long long;
const double EPS = 1e-8;
const ll MAXN = 1e18;
const int INF = 0x3f3f3f3f;
const int N = 1e6 + 5;

int a[N];


int main(int argc, char* argv[]) {
   
    std::ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    int n, x;
label:
    while(~scanf("%d", &n)) {
        if(!n) break;
        map<int, int> m;
        for(int i = 0; i < n; i++) {
            scanf("%d", &x);
            // m[a[i]]++;
            m[x]++;
        }
        double tmp = n / 2.0;
        for (auto i : m) {
            if (i.second > tmp) {
                printf("%d\n", i.first);
                goto label;
            }
        }
        puts("NO COLOR");
    }

    return 0;
}

