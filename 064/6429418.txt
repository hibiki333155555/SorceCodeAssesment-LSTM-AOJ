#include <iostream>
using namespace std;
typedef long long ll;

int main(){
    int a,b,c,d,e;
    cin >> a >> b >> c >> d >> e;
    
    int ans;
    if(a<0) ans+=c*abs(a)+d;
    ans+=e*b;
    cout << ans << end;
    return 0;
}    
