import math

while True:
    n = int(input())
    if n == 0: break
    s = list(map(int, input().split()))
    m = sum(s) / len(s)
    sm = 0 
    for i in s:
        sm += (i - m)**2
    a = math.sqrt(sm / n)

    print(f'{a:.08f}')

