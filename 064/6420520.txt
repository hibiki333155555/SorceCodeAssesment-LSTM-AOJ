#include <stdio.h>

int main()
{
	int i, L, N, a[101];
	scanf("%d %d", &L, &N);
	for (i = 1; i <= N; i++) {
		scanf("%d", &(a[i]));
		L -= a[i];
		if (L <= 0) break;
	}
	if (i <= N) printf("%d\n", i);
	else printf("No\n");
	fflush(stdout);
	return 0;
}
