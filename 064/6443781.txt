#include <stdio.h>
int main(void)
{
    int S,h,m,s;
    do
    {
        scanf("%d",&S);
    }while(!(0<=S&&S<86400));
    h = S / 60 / 60;
    m = S / 60;
    s - S;
    printf("%d:%d:%d\n",h,m,s);
    return 0;
}
