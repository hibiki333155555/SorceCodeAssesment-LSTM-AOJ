#include <iostream>
using namespace std;

int main()
{
    int a, b;
    int d, r;
    
    cin >> a >> b;
    
    d = a / b;
    r = a % b;
    double f = (double)a / (double)b;
    
    cout << d << " " << r << " " << f << endl;
    return 0;
}
