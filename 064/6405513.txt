require('fs').readFileSync(0,'utf8').split`
`.map(l=>{
    [H,W]=l.split` `.map(n=>+n);
    for(c='';H--;console.log(c+'\n'))
        for(;W--;)
            c += (H+W)%2?'.':'?';
    
    console.log('\n')
});
