#include<bits/stdc++.h>
using namespace std;
int main()
{
    vector<int>A;
    //vector<int>:: iterator it;
    //it=A.begin();
    long long int x,q,t,a,p;
    cin>>q;
    for(int i=1;i<=q;i++)
    {

            cin>>a;
            if(a==0)
            {
                    cin>>x;
                    A.push_back(x);
            }
            else if(a==1)
            {
                    cin>>p;
                    if(!A.empty())
                    {
                     cout<<A[p]<<endl;
                    }

            }
            else if (a==2)
            {
                    if(!A.empty())
                        {
                    A.pop_back();
                   // vector<int>::iterator it;
                    }
            }
    }

}

