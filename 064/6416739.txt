#include <bits/stdc++.h>
using namespace std;

int main() {
  int a,b;
  cin >> a >> b;

  int d,e;
  double f;

  d = a / b;
  e = a % b;
  f = (double)a / (float)b;

  cout << d << " " << e << " " << f << endl;
  
  return 0;
}
