require('fs').readFileSync(0,'utf8').split`
`.some(l=>{
    [H,W]=l.split` `.map(n=>+n);
    return H*W == 0;
    for(c='';H--;console.log(c+'\n'))
        for(;W--;)
            c += (H+W)%2?'#':'.';
    
    console.log('\n')
});
