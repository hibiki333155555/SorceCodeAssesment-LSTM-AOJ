#include <stdio.h>
#include <bits/stdc++.h>
using namespace std;
#define rep(i,n) for(int i=0;i<(n);i++)
#define Inf 1000000001

int main(){
	
	int L,N;
	cin>>L>>N;
	
	vector<int> a(N);
	rep(i,N)cin>>a[i];
	
	rep(i,N){
		L -= a[i];
		if(L<=0){
			cout<<i+1<<endl;
			return 0;
		}
	}
	cout<<"No"<<endl;
	
	return 0;
}
