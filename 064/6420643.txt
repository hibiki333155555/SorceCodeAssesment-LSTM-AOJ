#include <bits/stdc++.h>

#define rep(i, n) for(int i = 0; i < (n); i++)
#define all(x) x.begin(), x.end()

using ll = long long;
using namespace std;

int main() {
    ll h1, w1, h2, w2; cin >> h1 >> w1 >> h2 >> w2;

    ll h = h1 / h2 + (h1 % h2 ? 1 : 0);
    ll w = w1 / w2 + (w1 % w2 ? 1 : 0);

    cout << h * w << "\n";
}
