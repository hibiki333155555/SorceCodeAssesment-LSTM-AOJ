#include<bits/stdc++.h>
using namespace std;
int main(){
    double x1, y1, x2, y2;
    cin>>x1>>y1>>x2>>y2;
    double tei = abs(x2 - x1);
    double taka = abs(y2 - y1);
    cout << (double)sqrt(tei*tei + taka*taka)<<"\n";
    return(0);
}

