#include<stdio.h>
int main()
{
    int W,H,x,y,r,f,g;
    scanf("%d %d %d %d %d", &W, &H, &x, &y, &r);
    int f=W-r;
    int g=H-r;
    if (x<=f && x >=r &&y <=g && y>=r){
        printf("Yes\n");
   }
    else{
        printf("No\n");
    }
return 0;
}

