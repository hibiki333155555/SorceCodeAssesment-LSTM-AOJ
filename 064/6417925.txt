#include <algorithm>
#include <cmath>
#include <iomanip>
#include <iostream>
#include <iterator>
#include <numeric>
#include <string>
#include <vector>
using namespace std;


int main()
{
    
    int n;
    cin >> n;
    if(n <= 1)
        cout << 1 << endl;
    else
    {
        int a = 1, b=1,c;
        for(int i = 2; i <= n; i++)
        {
            c = a+b;
            b = a;
            a = c;
        }
        cout << c << endl;
    }
    return 0;
}
