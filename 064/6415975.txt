#include <iostream>
#include <iomanip>
#include <cmath>
#include <vector>
#include <cctype>
#include <string>
#include <sstream>
#include <array>
#include <algorithm>
#include <math.h>

using namespace std;

//int sctoi(char c);

int main(){
    cout << fixed;
    cout << setprecision(10);
    //vector<vector<int>> a(n, vector<int>(m));
    //getline(cin, s);
    size_t n=0;
    cin >> n;
    //array<int, 100> a;// s.fill(0.0);
    vector<int> a(n); //,  y(n); 
    for(auto &e : a) cin >> e;
    
    int v, j=0;
    for(size_t i=0; i<n; i++){
        v = a[i];
        j = i-1;
        while( j >= 0 && a[j] > v ){
           a[j+1] = a[j];
            j--;
            a[j+1] = v;
        }
        for(size_t j=0; j < n-1; j++) cout<<a[j]<<" ";
        cout<<a[n-1]<<endl;
    }
    return 0;
}
//reverse(s.begin() + a, s.begin() + b + 1 );
//s = s.replace(a, b-a+1, s2);
//if((tc.at(0) - 'a') == (hc.at(0) - 'a')){
//if(tc.compare(hc) == 0){
//s = s.substr(h, ssz - h) + s.substr(0, h); 
//transform(t.begin(), t.end(), t.begin(), ::tolower);
//isalpha(c) a.at(tolower(c) - 'a');
//s.find(p) == string::npos;
/*int sctoi(char c){
    stringstream ss;
    ss << c;
    int i;
    ss >> i;
    return i;
}
*/
