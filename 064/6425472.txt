#include <iostream>
#include <algorithm>
#include <iomanip>
#include <climits>
#include <string>
#include <cmath>

using namespace std;

int main()
{
int a,b;
while ( cin >>h >> w)
  if (h==0 or 0) break;
  for (int i =0; i <h; i++)
    {
      for (int j = 0; i < w; i++)
      {
        cout << '#'<<;
      }
      cout << endl;
    }
    cout << endl;
  }
