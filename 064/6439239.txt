#include <iostream>
#include <stdio.h>
#include <cmath>
#include <iomanip>
#include <stack>
#include <vector>
using namespace std;

int main(void){
    int n, m, l;
    vector< vector<int> > A;
    vector< vector<int> > B;
    vector< vector<int> > C;

    cin >> n >> m >> l;

    A.resize(n);
    B.resize(m);
    C.resize(n);

    for (int i=0; i<n; i++) A[i].resize(m);
    for (int i=0; i<m; i++) B[i].resize(l);
    for (int i=0; i<n; i++) C[i].resize(l);

    for (int i=0; i<n; i++) {
        for (int j =0; j<m; j++) {
            cin >> A[i][j];
        }
    }

    for (int i=0; i<m; i++) {
        for (int j =0; j<l; j++) {
            cin >> B[i][j];
        }
    }

    for (int i=0; i<n; i++) {
        for (int j=0; j<l; j++) {
            C[i][j] = 0;
            for (int k=0; k<m; k++){
                C[i][j] += A[i][k]*B[k][j];
            }
        }
    }
    return 0;
}
