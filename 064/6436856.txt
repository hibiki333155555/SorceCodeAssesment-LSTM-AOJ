#include <iostream>
using namespace std;

int main(void){
    int x, y;
    int t;

    for (int i=1; i<=3000; i++) {
        cin >> x;
        cin >> y;

/*
        if (x > y) {
            t = x;
            x = y;
            y = t;
        }
*/
        if (x < y) {
            cout << x << " " << y << endl;
        } else {
            cout << y << " " << x << endl;
        }
        //cout << x << " " << y << endl;
    }
    
    return 0;
}
