#include <iostream>
#include <algorithm>
#include <vector>
using namespace std;

int main(){
    int a, b, c;
    cin >> a >> b >> c;
    
    int num[3] = {a,b,c};
    sort(num, num + 3);
    
    cout << num[0] << " " << num[1] << " " << num[2] << endl;
    
    return 0;
}
