#include <stdio.h>
#include <stdlib.h>
int main(void){
    // Your code here!
    int n, m;
    scanf(" %d", &n);
    int * A = (int *)malloc(sizeof(int) * n);
    for (int i = 0; i < n; i++){
        scanf(" %d", &A[i]);
    }
    scanf(" %d", &m);
    int * B = (int *)malloc(sizeof(int) * m);
    for (int i = 0; i < m; i++){
        scanf(" %d", &B[i]);
    }
    
    /*for (int i = 0; i < n; i++){
        printf(" %d", A[i]);
    }
    printf("\n");
    for (int i = 0; i < m; i++){
        printf(" %d", B[i]);
    }
    printf("\n");*/
    
    int a = 0, b = 0, type;
    while(1){
        if (a == n || b == m) {
            if (a == n && b == m) type = 0;
            else if (a == n) type = 1;
            else if (b == m) type = 2;
            break;
        }
        
        if (A[a] < B[b]){
            printf("%d\n", A[a], a, b);
            if (a == n) a = n;
            else a++;
        } else if (A[a] > B[b]){
            printf("%d\n", B[b], a, b);
            if (b == m) b = m;
            else b++;
        } else {
            //printf("same %d\n", A[a]);
            if (a == n) a = n;
            else a++;
            if (b == m) b = m;
            else b++;
        }
    }
    
    //printf("type --> %d\n", type);
    if (type == 1) {
        for (int i = b; i < m; i++){
            printf("%d\n", B[i]);
        }
    } else if (type == 2){
        for (int i = a; i < n; i++){
            printf("%d\n", A[i]);
        }
    }
    return 0;
}
