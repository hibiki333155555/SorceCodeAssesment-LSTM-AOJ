#include <iostream>

using namespace std;
typedef long long ll;
ll h[2],w[2];
int main(){
    cin >> h[0] >> w[0] >> h[1] >> w[1];
    ll a = (h[0] + h[1] - 1)/h[1];
    ll b = (w[0] + w[1] - 1)/w[1];
    cout << a*b << endl;
}
