

#include <bits/stdc++.h>
#include <iostream>
#include <limits>
#include <numeric>
#include <type_traits>
#include <bitset>
#include <map>
#include <unordered_map>
#include <set>

using namespace std;

#define rep(i,n,m) for(ll (i)=(n);(i)<(m);(i)++)
#define rrep(i,n,m) for(ll (i)=(n);(i)>(m);(i)--)
using ll = long long;
const ll mod = 998244353;
const ll inf = 1e9;
const ll INF = 1e18;


void pline(vector<ll> lis){
    rep(i,0,lis.size()){
        printf ("%lld",lis[i]);
        if (i != lis.size()-1) printf(" ");
        else printf("\n");
    }
}

int main(){

    ios::sync_with_stdio(false);
    std::cin.tie(nullptr);

    //ModFac mf(200000);
    ll a,b,c;
    ll d,e,f;
    cin >> a >> b >> c;
    cin >> d >> e >> f;
    a = llabs(a);
    b = llabs(b);
    c = llabs(c);
    d = llabs(d);
    e = llabs(e);
    f = llabs(f);

    vector<ll> A = { (a^b)%2 , (b^c)%2 , (c^a)%2 };
    vector<ll> B = { (d^e)%2 , (e^f)%2 , (f^d)%2 };

    //pline(A);
    //pline(B);

    ll ans = 1;
    rep(i,0,3){
        if (A[i] != B[i]) ans = 0;
    }

    if (ans == 1){
        cout << "Yes\n";
    }else{
        cout << "No\n";
    }


}

/*

A:
1 -> 5 (4)
2 -> 6 (4)
3


D:
Mちっさ
これはやるだけ
ではないな Tがあるので
いややるだけだな

C:
グリッドを配列として持つことは可能だ
外周は √1e5 個しかない

E:
差の偶奇？

*/
