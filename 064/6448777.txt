#include <iostream>
#include <vector>
#include <map>
#include <algorithm>
#include <string>
#include <cstring>
#include <iomanip>
#include <cmath>
#include <stack>
#include <queue>
#include <list>
// #include "comp.h"
using namespace std;
using ll = long long;

// #define DEBUG
#define FOR(i, a, b) for (int i = (int)(a); i < (int)(b); ++i)
#define rep(i, n) for (int i = 0; i < (int)(n); ++i)

#define N 10000

int memo[N] = {0};

int fib(int n)
{
    int a[N];

    a[0] = 1;
    a[1] = 1;

    FOR(i, 2, n+1)
        a[i] = a[i-1] + a[i-2];
    
    return a[n];
}

int main()
{
#ifndef DEBUG
    int n;
    cin >> n;

    cout << fib(n) << endl;
#else

#endif
}

