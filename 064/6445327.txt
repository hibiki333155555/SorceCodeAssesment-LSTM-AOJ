#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
int main(){
    int a,b;
    while(1){
        cin >> a >> b;
        if(a==0 && b==0)break;
        cout << min(a,b) << ' ' << max(a,b) << endl;
    }
}
