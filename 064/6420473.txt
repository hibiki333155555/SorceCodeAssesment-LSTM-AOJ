#define rep(i,n) for(int i=0;i<(int)(n);i++)
#define ALL(v) v.begin(),v.end()
typedef long long ll;
 
#include <bits/stdc++.h>
using namespace std;

int main(){
  ios::sync_with_stdio(false);
  std::cin.tie(nullptr);

  ll h1,w1,h2,w2;
  cin>>h1>>w1>>h2>>w2;
  cout<<((h1+h2-1)/h2)*((w1+w2-1)/w2)<<endl;
  
  
  
  
  return 0;
}
