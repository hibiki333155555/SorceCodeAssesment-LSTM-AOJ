#include <iostream>
#include <vector>
using namespace std;

int H, W;
vector<vector<int>> field;

void dfs(int h,int w){
    seen[h][w] = true;
    

}

int main(){
    
    while(cin >> W >> H){
        if(H == 0 || W == 0){
            break;
        }
        field.assign(H, vector<int>(W, 0));
        for(int h = 0; h < H; h++){
            for(int w = 0; w < W; w++){
                cin >> field[h][w];
            }
        }

        int cnt = 0;
        for(int h = 0; h < H; h++){
            for(int w = 0; w < W; w++){
                if(field[h][w] == 0){
                    continue;
                }
                dfs(h,w);
                cnt++;
            }
        }
        cout << cnt << endl;

    }

}
