#include <bits/stdc++.h>
using namespace std;
int main(){
    int N, X;
    cin >> N >> X;
    int ans = 0;
    for(int i = 0; i < N; i++){
        int M;
        cin >> M;
        if(M >= X) ans++;
    }
    cout << ans << endl;
}
