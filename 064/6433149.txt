#include <bits/stdc++.h>
#include <vector>
using namespace std;

int main() {
    
    int x,y;
    while(1) {
        cin >> x >> y;
        if(x==0 && y==0) {
            break;
        }
        else {
            cout << y << " " << x << endl;
        }
    }
    return 0;
    
}
