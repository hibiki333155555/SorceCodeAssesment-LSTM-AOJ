/*-- 入力 --*/
//main("3\n5\n11\n0\n4\n4")
main(require("fs").readFileSync("/dev/stdin", "utf8"));

/*-- 計算&出力 --*/
function main(raw) {
    // 入力処理
    const args = raw.split("\n");
    const input = args[0].split(' ').map(Number);
    // 計算
    const num = 1;
    for(let i=0; i<args.length; i++){
        if(args[i]==0){break;}
        console.log(`Case ${i+1}: ${args[i]}`);
    };
    // 表示
    //console.log(`${output}`);
}

