#include <bits/stdc++.h>
using namespace std;
using ll=long long;
int main(){
  ll n,m,k,x;
  cin>>n>>m>>k>>x;
  vector<ll> a(n);
  ll ans=0;
  for(int i=0;i<n;i++){
    cin>>a[i];
    ans=max(ans,a[i]);
  }
  if(ans*k>=m*x){
    cout<<"Yes"<<endl;
  }
  else{
    cout<<"No"<<endl;
  }
}
