#include <stdio.h>
#include <stdlib.h>
#define _CRT_SECURE_NO_WARNINGS

int main(void) {
	int S;
	int s, m, h;

	scanf("%d", &S);
	
	h = S / 3600;
	m = (S % 3600) / 60;
	s = S % 60;
	
	printf("%d:%d:%d\n", h, m, s);
	
	return 0;
}
