"include <stdio.h>

int main(void)
{
    int a,b,c;
    do
    {
        scanf("%d %d %d",&a,&b,&c);
    }while(0 <= min(a,b,c) && max(a,b,c) <= 100);
    if(a < b && b < c)
    {
        printf("Yes\n");
    }
    else
    {
        printf("No\n");
    }
    return 0;
}
