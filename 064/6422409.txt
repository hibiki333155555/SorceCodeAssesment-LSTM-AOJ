#include <iostream>
#include <algorithm>

using namespace std;

int main() {
    int x, y;
    while (true) {
        cin >> x >> y;
        if (!x && !y) break;
        if (x > y) {
            swap(x, y);
        }
        cout << x << " " << y << endl;
    }
    return 0;
}

