#include<stdio.h>
int main ()
{
    int i,b,c,d,k;
    int a[100];
    int key;
    scanf ("%d",&c);
    for(int i=0;i<c;i++)
    scanf ("%d",&i);
    for (int b=0;b<c;b++)
    {
        key=a[b];
        i=b-1;
    }
      while(i>0&&a[i]>a[i+1])
      {
        a[i]=a[i+1];
          i--;
      }
       for(int k=0;k<c;k++)
      {
       if (k==c)
       printf ("%d",&a[k]);
      }
      return 0;
}
    
