#include<stdio.h>

int main()
{
    int w,h,x,y,r,a,b,c,d;
    
    scanf("%d %d %d %d %d", 
    &w,&h,&x,&y,&r);
    a=x-r;
    b=x+r;
    c=y-r;
    d=y+r;
    if(a>=0 && b<=w && c>=0 && d<=h)
    printf("Yes\n");
    else printf("No\n");
    return 0;
}
