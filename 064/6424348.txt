#include <iostream>
#include <stdio.h>
#include <cmath>

#define ll long long

using namespace std;

int main(){
    int a,b;
    cin >> a >> b;
    string ans;
    if (a==b) ans = "a == b";
    else if (a>b) ans = "a > b";
    else ans = "a < b";
    cout << ans << endl;
    //system("pause");
    return 0;
}
