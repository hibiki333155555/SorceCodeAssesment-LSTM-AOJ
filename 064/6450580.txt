#include <bits/stdc++.h>

using namespace std;

#define REP(i,n) for(int i=0;i<int(n);i++)
typedef long long ll;
typedef pair<int,int> P;


int main(void){
	int i,j,k;

	cin.tie(0);  ios_base::sync_with_stdio(false);

	int r1,r2;
	string c1,c2;
	cin >> c1 >> r1 >> c2 >> r2;

	int x,y;
	if((int)c1.size()==1) x=c1[0]-'A'+1;
	else x=26*(c1[0]-'A'+1)+c1[1]-'A'+1;

	if((int)c2.size()==1) y=c2[0]-'A'+1;
	else y=26*(c2[0]-'A'+1)+c2[1]-'A'+1;

	cout << (r2-r1+1) *(y-x+1)<< endl;


	return 0;
}

