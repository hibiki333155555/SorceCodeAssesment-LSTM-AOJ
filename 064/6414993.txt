#include<stdio.h>
#include<string.h>

int main(void)
{
    char s[202],p[101],r[202];
    scanf("%s %s",s,p);
    strcpy(r,s);
    strcat(s,r);
    if(strstr(s,p)==NULL)printf("No\n");
    else printf("Yes\n");
    return 0;
}
