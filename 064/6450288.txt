#include <bits/stdc++.h>

using namespace std;

#define REP(i,n) for(int i=0;i<int(n);i++)
typedef long long ll;
typedef pair<int,int> P;

string r[110];

int main(void){
	int i,j,k;

	cin.tie(0);  ios_base::sync_with_stdio(false);

	int N;
	cin >> N;
	for(i=1;i<=N;i++) cin >> r[i];

	int n=r[1].size();
	set<string> s;
	for(i=1;i<N;i++){
		for(j=i+1;j<=N;j++){
			int d=0;
			for(k=0;k<n;k++) if(r[i][k]!=r[j][k]) d++;
			if(d<=1){
				s.insert(r[i]);
				s.insert(r[j]);
			}
		}
	}

	cout << s.size() << endl;

	return 0;
}

