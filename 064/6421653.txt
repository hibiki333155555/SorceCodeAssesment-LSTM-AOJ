#include <iostream>

using namespace std;

int main()
{
    int H = 0;
    int W = 0;
    cin>>H>>W;
    while(W != 0 && H != 0)
    {
    for(int x = 0;x < W;x++)
    {
        cout<<"#";
    }
    cout<<endl;

        for(int i = 0;i > 1 && i < H-1;i++)
        {
            cout<<"#";
            for(int j = 0;j > 1 && j < H-1;j++)
            {
                cout<<".";
            }
            cout<<"#";
        }
    for(int y = 0; y < H;y++)
        {
            cout<<"#";
        }
    cout<<endl;
    cin>>H>>W;
    }

    return 0;
}


