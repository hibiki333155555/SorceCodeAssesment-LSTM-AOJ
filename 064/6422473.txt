let [x, y] = require(`fs`).readFileSync(`/dev/stdin`, `utf-8`).split(` `);

function gcd(a, b) {
    if (b == 0) console.log(a);
    else if (a >= b) gcd(b, a % b);
    else if (a < b) gcd(b, a);
}

gcd(x, y);

