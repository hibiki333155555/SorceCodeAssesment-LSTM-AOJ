#include <iostream>
#include <math.h>

using namespace std;

int n;
int a[200];

int main()
{
    double a, b, c;
    double h, x;
    double rad;
    double PI = acos(-1);
    cin >> a >> b >> c;
    rad = c * PI / 180;
    h = sin(rad) * b;
    x = (a * a) + (b * b) - 2 * a * b * cos(rad);
    cout << (a * h) / 2 << " " << sqrt(x) + a + b << " " << h << endl;
    return 0;
}

