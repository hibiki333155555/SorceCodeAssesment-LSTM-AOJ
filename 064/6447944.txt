#include<stdio.h>
int main()  {
    int n;
    scanf("%d", &n);
    int num[n];
    int sum = 0;
    int count = 0;
    for (int i=0;i<n;i++)  {
        scanf("%d\n", &num[i]);
        }
 for (int i=0;i<n;i++) {
     for (int j=0;j<num[j]/2;j+=2)  {
         if (num[j]%j==0) {
             count++;
         }
     }
    if (count==0) {
      sum+=1;
    }
 }
   printf("%d", sum); 
return 0;
 }
