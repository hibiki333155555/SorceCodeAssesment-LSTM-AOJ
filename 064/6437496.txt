#include <iostream>
#include <algorithm>
#include <iomanip>
#include <climits>
#include <string>

using namespace std;

int main()
{
string a; cin >> a;
// change a to uppercase
for (char& c : a) c = toupper(c);
string b;
int count = 0;
  while (cin >> b)
    {
      
      if (b == "END_OF_TEXT") break;
      for (char& c : b) c = toupper(c);
      if (b == a)
      {
        count++;
      }
    }
  cout << count << endl;
  }
