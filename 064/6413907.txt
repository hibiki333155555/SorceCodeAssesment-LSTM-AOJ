#include<iostream>
using namespace std;

int main()
{
	int m,f,r,i;
	int score[50][1][1][1];
    char grade[50];

	for(i=0;i<50;++i){
        cin>>score[i][m][f][r];
        if(m==-1 && f==-1 &&r==-1)break;
    }


	for(int j = 0; j < i; j++){
        if(m==-1 || f==-1)grade[i]=='F';
        else if((m+f)>=80)grade[i]=='A';
        else if((m+f)>=65 && (m+f)<80)grade[i]=='B';
        else if((m+f)>=50 && (m+f)<65)grade[i]=='C';
        else if((m+f)>=30 && (m+f)<50 && r>=50)grade[i]=='C';
        else if((m+f)>=30 && (m+f)<50)grade[i]=='D';
        else if((m+f)<30)grade[i]=='F';
        
    }

    for(int i = 0; i < 50; i++){
        cout<<grade[i]<<endl;
    }

return 0;
}      
