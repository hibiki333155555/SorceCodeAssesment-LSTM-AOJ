import math
while True:
    n = int(input())
    if n == 0:
        break
    else:
        score = list(map(int,input().split()))
        m = sum(score) / len(score)
        v = [(i - m)**2 for i in score]
        v = sum(v) / n
        s = math.sqrt(v)
        print(f'{s:.10f}')
