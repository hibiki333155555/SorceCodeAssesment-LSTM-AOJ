#include<stdio.h>

int main(){
    int sum, n, tem;
    int max = 0, min = 0;
    
    scanf("%d",&n);
    
    for(int i = 0; i < n; i++){
        scanf("%d",&tem);
        sum += tem;
        if(max < tem)max = tem;
        if(min > tem)min = tem;
    }
    printf("%d %d %d\n", min, max, sum);
    return 0;
}
