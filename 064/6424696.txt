#include <iostream>
#include <algorithm>
using namespace std;

int main()
{
   int n, a, b;
   string s;
   cin >> n >> a >> b >> s;
   if (a != b)
      reverse(s.begin() + a-1, s.begin() + b);
      
   cout << s << endl;
   return 0;
}
