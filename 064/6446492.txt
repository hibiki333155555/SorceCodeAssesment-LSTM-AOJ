#include <bits/stdc++.h>
using namespace std;

int main() {
  int W, H, x, y, r;
  cin >> W >> H >> x >> y >> r;
  
  if (x < W && y < H && r < sqrt((W - x) * (W -x) + (H -y) * (H -y))) {
    cout << "Yes" << endl;
  }
  else {
    cout << "No" << endl;
  }
}
