#include<iostream>
#include<string>
#include<algorithm>
using namespace std;
int main(){
    string T, P; cin >> T >> P;
    auto it = T.begin() - 1;
    while(1){
        it = search(it+1, T.end(), P.begin(), P.end());
        if(it == T.end()) break;
        cout << it - T.begin() << endl;
    }
    return 0;
}
