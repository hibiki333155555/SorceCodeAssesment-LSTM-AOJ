c='';
require('fs').readFileSync(0,'utf8').split`
`.map(l=>{
    [H,W]=l.split` `.map(n=>+n);
    if(H===0) return;
    for(;H--;) console.log('#.'.repeat(W).substr(0,W)+'\n');
    console.log('\n')
})
