#include <algorithm>
#include <iostream>
using namespace std;

int main(){
  long long n, m, k, x;
  cin >> n >> m >> k >> x;
  long long a_max = 0;
  for(int i = 0; i < n; ++i){
    long long a;
    cin >> a;
    a_max = max(a,a_max);
  }
  cout << (a_max*k >= m*x ? "Yes" : "No") << endl;
}

