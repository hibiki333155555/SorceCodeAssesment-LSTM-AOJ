#include <bits/stdc++.h>
using namespace std;
 
int main() {
  vector<int> vec(3);
 
  // atを使って1つずつ入力
  cin >> vec.at(0) >> vec.at(1) >> vec.at(2);
  sort(vec.begin(), vec.end());
  
  for (int i = 0; i < 3; i++) {
        cout << vec.at(i) << " ";
    }
  
}
