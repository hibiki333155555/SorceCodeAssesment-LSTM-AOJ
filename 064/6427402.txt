#include<bits/stdc++.h>
using namespace std;
#define rep(i,n) for(int i=0;i<(n);i++)
//using ll=long long;
int main(){
    int ans=0,w=0,q=0;
    vector<int> a(4);
    rep(i,4){
        cin>>a[i];
    }
    sort(a.begin(),a.end());
    if(a[0]==a[1]&&a[2]==a[3])cout<<"yes"<<"\n";
    else cout<<"no"<<"\n";
    
}
