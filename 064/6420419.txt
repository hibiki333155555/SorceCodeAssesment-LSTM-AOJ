#include <bits/stdc++.h>
using namespace std;
int main(){
  int H1, W1;
  cin >> H1 >> W1;
  int H2, W2;
  cin >> H2 >> W2;
  int x = (H1 + H2 - 1) / H2;
  int y = (W1 + W2 - 1) / W2;
  cout << (long long) x * y << endl;
}
