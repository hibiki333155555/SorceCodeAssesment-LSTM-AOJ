#include <iostream>
#include <algorithm>
#include <string>

using namespace std;

int main(){
    string key, str;
    getline(cin, key);
    
    int cnt = 0;
    while(getline(cin, str)){
        cnt += count(str.begin(), str.end(), key);
    }
    cout << cnt << endl;
    return 0;
}
