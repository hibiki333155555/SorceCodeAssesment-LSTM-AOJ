#include <bits/stdc++.h>
using namespace std;

using Int = long long;
const char newl = '\n';

template<typename T1,typename T2> inline void chmin(T1 &a,T2 b){if(a>b) a=b;}
template<typename T1,typename T2> inline void chmax(T1 &a,T2 b){if(a<b) a=b;}
template<typename T> void drop(const T &x){cout<<x<<endl;exit(0);}
template<typename T=Int>
vector<T> read(size_t n){
  vector<T> ts(n);
  for(size_t i=0;i<n;i++) cin>>ts[i];
  return ts;
}

//INSERT ABOVE HERE
signed main(){
  cin.tie(0);
  ios::sync_with_stdio(0);

  string c1,c2;
  Int r1,r2;
  cin>>c1>>r1>>c2>>r2;

  vector<string> cs;
  auto idx=[&](string s){
    return find(cs.begin(),cs.end(),s)-cs.begin();
  };

  for(Int i=0;i<26;i++)
    cs.emplace_back(""s+char('A'+i));

  for(Int i=0;i<26;i++)
    for(Int j=0;j<26;j++)
      cs.emplace_back(""s+char('A'+i)+""s+char('A'+j));

  cout<<(idx(c2)-idx(c1)+1)*(r2-r1+1)<<endl;
  return 0;
}

