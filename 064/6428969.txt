#include <stdio.h>

int main(void) {
	int a, b, c;
	
	/*入力*/
	scanf("%d %d %d", &a, &b, &c);
	
	/*処理*/
	if ( a < b ) {
		if ( a > c ) {
			printf("%d %d %d", c, a, b);
		} else if ( b < c ) {
			printf("%d %d %d", a, b, c);
		} else {
			printf("%d %d %d", a, c, b);
		}
	} else {
		if ( b > c ) {
			printf("%d %d %d", c, b, a);
		} else if ( a < c ) {
			printf("%d %d %d", b, a, c);
		} else {
			printf("%d %d %d", b, c, a);
		}
	}
	
	return 0;
}
