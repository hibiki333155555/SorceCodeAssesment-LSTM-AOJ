#include <iostream>
#include <array>
#include <algorithm>
using namespace std;

int main()
{
   array<int, 3> arr;
   for (int i = 0; i < 3; ++i)
      cin >> arr[i];
   sort(arr.rbegin(), arr.rend());
   cout << arr[1] << endl;
   return 0;
}
