#include <bits/stdc++.h>
using namespace std;
int main() 
{
	vector<int> v;
	int t,m,b,c;
	cin>>t;
	for(int i=0;i<t;i++)
	{
		cin>>m;
		if(m==2)
		{
			v.pop_back();
		}
		else if(m==0)
		{
			cin>>b;
			v.push_back(b);
		}
		else if(m==1)
		{
			cin>>c;
			cout<<v[c]<<endl;
		}
	}
return 0;
}
