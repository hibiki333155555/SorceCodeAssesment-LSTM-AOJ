#include<bits/stdc++.h>
#define MYTUL ios_base::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);
using namespace std;
int main()
{
    MYTUL
    int t; cin>>t;
    int x;
    map<string, int>mp;
    for(int i=0; i<t; i++)
    {
         cin>>x;
        if(x==0)
        {
            string key;
            int val;
            cin>>key>>val;
            mp[key]=val;
        }
        else if(x==1)
        {
            string s;
            cin>>s;
            if(mp.find(s)==mp.end())

                cout<<0<<"\n";
            else
                cout<<mp[s]<<"\n";
        }
        else if(x==2)
        {
            string s;
            cin>>s;
            mp.erase(s);
        }
        else if(x==3)
        {
            string p,y;
            cin>>p>>y;

                map<string,int>::iterator lo,up,itr;
                lo = mp.lower_bound(p);
                up = mp.upper_bound(y);
                for( itr = lo; itr!=up; itr++)
                cout<<itr->first<<" "<<itr->second<<"\n";
        }
    }
}
