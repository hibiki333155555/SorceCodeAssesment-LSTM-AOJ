#include <bits/stdc++.h>
using namespace std;

int main() {
  int a, b, c;
  cin >> a >> b >> c;

  int tmp;

  if(a > b && b > c){

  }else if(a < b && b < c){
    tmp = a;
    a = c;
    c = tmp;
  }else{
    if(a > c){
      tmp = a;
      a = b;
      b = tmp;
    }else if(a < c){
      tmp = a;
      a = b;
      b = c;
      c = tmp;
    }
  }

  cout << c << b << a << endl;
  return 0;
}
