#include<stdio.h>
#include<string.h>

int main(){
    
    char s1,s2,p;
    
    scanf("%s", &s1);
    
    strcpy(s1,s2);
    strcat(s1,s2);
    
    scanf("%s", p);
    
    if(strstr(strcat(s1,s2),p) == NULL)printf("No\n");
    else printf("Yes\n");
    
    return 0;
}
