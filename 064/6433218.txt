#include <stdio.h>

int main()
{
    int ls, h, m, s;

    scanf("%d", &ls);
    
    h = ls / 3600;
    m = (ls - h * 3600) / 60;
    s = (ls - h * 3600 - m * 60);
    
    printf("%d:%d:%d\n", h, m, s);

    return 0;
}
