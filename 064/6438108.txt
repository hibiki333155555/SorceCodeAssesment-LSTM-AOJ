#include <bits/stdc++.h>
using namespace std;
 
int main() {
  double x1, y1, x2, y2;
  cin >> x1 >> y1 >> x2 >> y2;
  
  double x, y;
  x = abs(x1 - x2);
  y = abs(y1 - y2);
  
  double distance;
  distance = sqrt(x * x + y * y);
  
  cout << fixed << setprecision(8) << distance << endl;
  
}
