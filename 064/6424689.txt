#include <bits/stdc++.h>

#define ll long long
#define ld long double
#define rep(i,a,b) for (int i = a; i < b; i++)
#define irep(i,a,b) for (int i = a; i > b; i--)

using namespace std;

int main(){
    int count = 0;
    int a,b,c;
    cin >> a >> b >> c;
    rep(i,a,b+1){
        if (c%i == 0) count += 1;
    }
    cout << count << endl;
    //system("pause");
    return 0;
}
