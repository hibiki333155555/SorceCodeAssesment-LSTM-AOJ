#include <stdio.h>

int main()
{
    int x, answer;
    
    printf("Enter any integers\n");
    scanf("%d", &x);
    
    if (x < 1 || x > 100)
    {
     answer = x * x * x;   
    }
    else
    {
        pritf("Invalid input\n");
        return 0;
    }
    
    printf("%d\n", answer);
    
    
    
    
    return 0;
}
