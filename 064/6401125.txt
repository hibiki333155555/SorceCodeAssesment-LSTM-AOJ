#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

int n;
int arr[101];

int main(){
  cin >> n;
  for (int i = 0; i < n; i++){
    cin >> arr[i];
  }
  for (int i = n - 1; i >= 0; i--){
    if (i == 0) cout << arr[i];
    else cout << arr[i] << ' ';
  }
  cout << endl;
}

