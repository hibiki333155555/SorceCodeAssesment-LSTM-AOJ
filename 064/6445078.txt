#include <iostream>
#include <string>
#include <vector>
#include <algorithm>
#include <map>

using namespace std;

class MyDictionary
{
private:
  map<char, vector<string>> book;
public:
  void insert(string str)
  {
    book[str[0]].push_back(str);
    sort( book[str[0]].begin(), book[str[0]].end() );
  }
  void find(string str)
  {
    vector<string> strlist = book[str[0]];
    int left = 0;
    int right = strlist.size();
    while (left < right)
    {
      int mid = (left + right) / 2;
      if (str == strlist[mid])
      {
        cout << "yes" << endl;
        return;
      }
      else if (str < strlist[mid]) right = mid;
      else left = mid + 1;
    }
    
    cout << "no" << endl;
    return;
  }
};

int main(int argc, char const *argv[])
{
  int n;
  cin >> n;

  MyDictionary myDict;
  for (int i = 0; i < n; i++)
  {
    string com;
    string op;
    cin >> com >> op;

    if (com[0] == 'i') myDict.insert(op);
    if (com[0] == 'f') myDict.find(op);
  }

  return 0;
}

