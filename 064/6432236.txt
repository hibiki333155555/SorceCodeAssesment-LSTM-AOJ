#include<bits/stdc++.h>
using namespace std;
int main()
{
    list<int>A;
    list<int>:: iterator it;
    it=A.begin();
    int x,q,a,d,y;
    cin>>q;
    for(int i=1; i<=q; i++)
    {

        cin>>a;
        if(a==0)
        {
            cin>>y;
            it =A.insert(it,y);
        }
        else if(a==1)
        {
            cin>>d;
            if(d>0)
            {
                for(int i=0; i<d; i++)
                {
                    it++;
                }
            }
            else 
            {
                for(int i=0; i<d; i++)
                {
                    it--;
                }
            }
        }
        else if (a==2)
        {

                it=A.erase(it);

    }
    }
    for(auto it=A.begin(); it!=A.end(); it++)
    {
        cout<<*it<<endl;
    }

}

/*
5
0 1
0 2
0 3
1 1
2
*/

