#include <stdio.h>

int staq (int *a)
{
	int i;
	int j;

	i = -1;
	j = -1;
	while ((a[++j] > -1000000001 && a[j] < 1000000001) || a[j] == (int)+ || a[j] == - || a[j] == * || a[j] == / || a[j] == %)
	{
		if (a[j + 2] == +)
			a[++i] = a[j] + a[j + 1];
		else if (a[j + 2] == -)
			a[++i] = a[j] - a[j + 1];
		else if (a[j + 2] == *)
			a[++i] = a[j] * a[j + 1];
		else if (a[j + 2] == /)
			a[++i] = a[j] / a[j + 1];
		else if (a[j + 2] == %)
			a[++i] = a[j] % a[j + 1];
		else if (a[j + 1] == +)
			a[--i] = a[i] + a[i + 1];
		else if (a[j + 1] == -)
			a[--i] = a[i] - a[i + 1];
		else if (a[j + 1] == *)
			a[--i] = a[i] * a[i + 1];
		else if (a[j + 1] == /)
			a[--i] = a[i] / a[i + 1];
		else if (a[j + 1] == %)
			a[--i] = a[i] % a[i + 1];
		else if (a[j] == +)
			a[--i] = a[i] + a[i + 1];
		else if (a[j] == -)
			a[--i] = a[i] - a[i + 1];
		else if (a[j] == *)
			a[--i] = a[i] * a[i + 1];
		else if (a[j] == /)
			a[--i] = a[i] / a[i + 1];
		else if (a[j] == %)
			a[--i] = a[i] % a[i + 1];
		else
			return (0);
		j += 3;
	}
	return (0);
}

int main(void)
{
	int i;
	int j;
	int g[200] = {-1000000001};
	int x;
	int num[1000000];
	int swap;

	i = -1;
	while (++i < n)
		scanf("%d", &g[i]);
	stap(g);
	printf("%d\n", g[0]);
	re
