#include <stdio.h>

int main(int argc, const char* argv[])
{
    int a, b, x, y;
    
    scanf("%d %d", &a, &b);
    
    x = a * b;
    
    y = (a + b) * 2;
    
    printf("%d %d", x, y);
    
    return 0;
}
