#include<stdio.h>
#include<math.h>

int main(void)
{
    double PI=acos(-1);
    int a,b,c;
    double s,l,h;
    scanf("%d %d %d",&a,&b,&c);
    double rad =c*PI/180;
    s=a*b*sin(rad)/2;
    l=sqrt(a*a+b*b-2*a*b*cos(rad));
    h=b*sin(rad);
    printf("%lf %lf %lf\n",s,l,h);
    return 0;
}
