'2 2\n1 1\n 0 0'.split`
`.map(l=>{
    if(l==='0 0') return;
    
    [H,W]=l.split` `.map(n=>+n);
	a=[];
    for(i=0;i<H;) a[i]=(i++%2? '.#' : '#.').repeat(W).substr(0,W);
    console.log(a.join`\n`+'\n');
    return;
})
