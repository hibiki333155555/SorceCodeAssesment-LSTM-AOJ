#include <iostream>
#include <vector>
#include <map>
// #include "comp.h"
using namespace std;

#define FOR(i, a, b) for (int i = (int)(a); i < (int)(b); ++i)
#define rep(i, n) for (int i = 0; i < (int)(n); ++i)

int main()
{

    int a, b;
    while (true)
    {
        scanf("%d %d", &a, &b);
        if (a == 0 && b == 0)
            break;
        if (a > b)
            printf("%d %d\n", b, a);
        else
        {
            printf("%d %d\n", a, b);
        }
    }
}

