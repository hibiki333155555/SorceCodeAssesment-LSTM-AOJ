c='';
require('fs').readFileSync(0,'utf8').split`
`.map(l=>{
    if(l==='0 0') return;
    [H,W]=l.split` `.map(n=>+n);
    for(;H--;) console.log((H%2?'.#':'#.').repeat(W).substr(0,W)+'\n');
    console.log('\n');
    
    return;
})
