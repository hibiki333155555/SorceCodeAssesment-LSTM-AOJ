#include <bits/stdc++.h>
using namespace std;
int main(){
    int N,K;
    cin>>N>>K;
    int z,t;
    for(int i=1;i<=K;i++)cin>>z,t+=z;
    if(t%(N+1)!=0)t+=(N+1);
    cout<<(t)/(N+1);
    return(0);
}
