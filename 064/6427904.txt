#include <stdio.h>
int main()
{
    int cas = 1, x;
    while (1)
    {
        scanf("%d", &x);
        if (x == 0)
        {
            break;
        }
        printf("case %d: %d\n", cas, x);
        cas++; /* code */
    }
    return 0;
}
