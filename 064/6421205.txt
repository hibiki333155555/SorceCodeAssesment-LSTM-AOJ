#include <bits/stdc++.h>
#define rep(i,n) for(int i = 0; i < (n); i++)
using namespace std;
typedef long long ll;

int main(){
    cin.tie(0);
    ios::sync_with_stdio(0);
    
    int L,N; cin >> L >> N;
    rep(i,N) {
        int a; cin >> a;
        L -= a;
        if(L <= 0) {
            cout << i + 1 << endl; return 0;
        }
    }
    cout << "No" << endl;
}
