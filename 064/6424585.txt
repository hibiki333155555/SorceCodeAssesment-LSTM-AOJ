#include <bits/stdc++.h>

#define ll long long
#define ld long double
#define rep(i,a,b) for (int i = a; i < b; i++)
#define irep(i,a,b) for (int i = a; i > b; i--)

using namespace std;

int main(){
    int n;
    cin >> n;
    vector<int> a(n);
    rep(i,0,n) cin >> a[i];
    irep(i,n-1,-1){
        cout << a[i];
        if (i != 0) cout << " ";
    }
    cout << endl;
    system("pause");
    return 0;
}
