#include<iostream>
using namespace std;

int main(){
    int x, y;
    cin >> x >> y;
    
    while (1) {
        cout << x << " " << y << endl;
        cin >> x >> y;
        if ( x == 0 && y == 0 ) break;  
    }

    return 0;
}
