#include <stdio.h>
int main (void)
{
    int i,j,N,k;
    int key;
    int a[100];
    scanf ("%d\n",&N);
    for(int i=0;i<N;i++)
    scanf ("%d\n" ,&a[i]);
    for (int j=1;j<N;j++)
    {
        a[j]=key;
        i=j-1;
        while(i>0&&a[i]>a[i+1])
        {
            a[i+1]=a[i];
            i--;
        }
          a[i+1]=key;
          for (k=1;k<N;k++)
          {
        if(k == N)printf("%d\n",a[k]);
		else printf("%d ",a[k]);
          }
    }
    
  return 0;  
    
}
