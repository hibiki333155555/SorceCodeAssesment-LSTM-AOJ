#include <stdio.h>
#include<iostream>
using namespace std;

int main(){
    int a;
    char op;
    int b, r;
    cin >> a >> op >> b >> r;
    
    while (op == '?') {
        if (op=='+'){
            r=a+b;
        } else if (op == '-'){
            r=a-b;
        } else if (op == '*'){
            r=a*b;
        } else {
            r=a/b;
        }
        cout << r << endl;
        cin >> a >> op >> b >> r;
    }
    
    // cout << c << " " << d << " " << e << endl;

    return 0;
}


