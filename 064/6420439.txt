// #pragma GCC target("avx2")
#pragma GCC optimize("O3")
// #pragma GCC optimize("unroll-loops")
#include<bits/stdc++.h>
using namespace std;


int main() {
    cin.tie(0);
    ios::sync_with_stdio(0);
    long long h1, w1, h2, w2;
    cin >> h1 >> w1 >> h2 >> w2;
    cout << ((h1 + h2 - 1) / h2) * ((w1 + w2 - 1) / w2) << '\n';



    return 0;
}

