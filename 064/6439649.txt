#include <iostream>
#include <string>
using namespace std;

class SimpleClass
{
private:
    int number;
    std::string name;
};

int main() {
    Dice d;
    string order;
    d.inputN();
    std::cin >> order;
    for (int i=0;i<order.length();i++) {
        switch(order[i]) {
            case 'W':
               d.w();
               break;
            case 'E':
              d.e();
              break;
            case 'N':
              d.n();
              break;
            case 'S':
              d.s();
              break;
            default:
              break;
        }
    }
    cout << d.top() << endl;
}
