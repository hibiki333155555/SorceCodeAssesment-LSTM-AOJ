const x = require('fs').readFileSync('/dev/stdin', 'utf8');
const arr = x.split(' ');
const area = arr[0] * arr[1];
const p = (arr[0] + arr[1]) * 2
const ans = `${area.toString} ${p.toString}`
console.log(ans)
