#include<iostream>
using namespace std;


int main(void){
	int maxn = 0, minn;
	int i;
	int n;
	//int T = -1000000000;
	int a[200005];
	cin >> n;
	for(i = 0; i < n; i++){
		cin >> a[i];
	}
	maxn = a[1] - a[0];
	minn = a[0];
	for(i = 1; i < n; i++){
	maxn = max(maxn, a[i] - minn);
	minn = min(a[i], minn);
	}
	/*if (maxn == 0){
		maxn = a[n - 1] - a[n - 2];
	
*/	//}
	cout << maxn << "\n";
	return 0;
} 
