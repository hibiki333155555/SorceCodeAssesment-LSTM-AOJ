#define _GLIBCXX_DEBUG
#include<iostream>
#include<stdio.h>
using namespace std;

bool search(int m, int i, vector<int>& A){
    if(m==0)
        return true;
    else if(i==n)
        return false;
    
    return search(m,i+1,A) || search(m-A[i],i+1,A);
}

int main(){
    int n,q,m;
    scanf("%d",n);
    
    vector<int>A(n);
    for(int i=0;i<n;i++)
        scanf("%d",A[i]);
    
    scanf("%d",q);
    
    for(int i=0;i<q;i++){
        scanf("%d",&m);
        if(search(m,0,A))
            printf("yes");
        else
            printf("no");
    }
    
    return 0;
}
