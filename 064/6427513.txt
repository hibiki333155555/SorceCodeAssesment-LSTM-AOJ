#include <bits/stdc++.h>
using namespace std;

int main()
{

    int n, mark=0;
    cin>> n;
    int arr[n+5];
    for(int i=0; i<n; i++) {
        cin>> arr[i];
    }

    int m;
    cin>> m;
    int arr2[m+5];
    for(int i=0; i<m; i++) {
        cin>> arr2[i];
    }

    if(n> m)
    {
             for(int i=0; i<m; i++) {
            if(arr[i] < arr2[i]) {
                mark= 1;
                break;
            }

            else if(arr[i] > arr2[i]) {
                mark= 2;
                break;
            }

        }

    }

    else {
            for(int i=0; i<n; i++) {
            if(arr[i] < arr2[i]) {
                mark= 1;
                break;
            }

            else if(arr[i] > arr2[i]) {
                mark= 2;
                break;
            }
        }
    }
    if(mark== 0)
    {
        if(n<m)
            mark= 1;
        else
            mark= 2;
    }

    if(mark==2)
    mark=0;
    cout<< mark<< endl;

    return 0;
}

