<?php

fscanf(STDIN, '%d', $N);
for ($i=0; $i < $N; $i++) { 
    fscanf(STDIN, '%d', $R[$i]);
}

$min = 1000000000;
$max = -$min;
for ($i=0; $i < $N; $i++) { 
    $max = max($max,$R[$i]-$min);
    $min = min($min,$R[$i]);
}

printf("%d\n",$max);
