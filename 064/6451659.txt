/*-- 入力 --*/
//main("3\n5\n11")
main(require("fs").readFileSync("/dev/stdin", "utf8"));

/*-- 計算&出力 --*/
function main(raw) {
    // 入力処理
    const args = raw.split("\n");
    const input = args[0].split(' ').map(Number);
    // 計算
    const num = 1;
    args.forEach( (val, idx) => {
        console.log(`Case ${idx+1}: ${val}`);
    });
    // 表示
    //console.log(`${output}`);
}

