#include <iostream>
using namespace std;

int main() {
   while(true) {
      int x=0, y=0, a;
      cin >> x >> y;
      if(x == 0) break;
      if(x > y) {
         a = x;
         x = y;
         y = a;
      }
      cout << x << " " << y << "\n";
   }
}
