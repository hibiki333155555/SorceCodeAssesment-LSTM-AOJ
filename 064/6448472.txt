#include <bits/stdc++.h>
using namespace std;

int main() {
	int n = 0;
	int m = 0;
	while (1) {
		scanf("%d %d", &n, &m);
		if (n == 0 && m == 0)
			break;
		printf("%d %d\n", n < m ? n : m, n > m ? n : m);
	}

	return (0);
}

