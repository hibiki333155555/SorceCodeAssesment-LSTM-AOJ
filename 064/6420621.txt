#pragma GCC target("avx2")
#pragma GCC optimize("O3")
#pragma GCC optimize("unroll-loops")
#include<bits/stdc++.h>
using namespace std;

using ll = long long;
const int INF = 1e9;
const ll inf = 1LL<<62;

void solve() {
  ll h1, w1, h2, w2; cin >> h1 >> w1 >> h2 >> w2;
  ll ans = ((h1 + h2 - 1)/h2)*((w1 + w2 - 1)/w2);
  // swap(h2, w2);
  // ans = min(ans, ((h1 + h2 - 1)/h2)*((w1 + w2 - 1)/w2));
  cout << ans << '\n';
}

int main() {
  ios::sync_with_stdio(false);
  std::cin.tie(nullptr);
  // int t; cin >> t;
  /*while (t--)*/ solve();
}

