#include<bits/stdc++.h>
using namespace std;
#define rep(i,n) for(int i=0;i<(n);i++)
//using ll=long long;
int main(){
    int n;cin>>n;
    int k[n],p[n];
    rep(i,n)cin>>k[i]>>p[i];
    rep(i,n){
        if(k[i]==p[i])cout<<k[i]<<"\n";
        else if(k[i]%p[i]==0)cout<<p[i]<<"\n";
        else cout<<k[i]%p[i]<<"\n";
    }
}
