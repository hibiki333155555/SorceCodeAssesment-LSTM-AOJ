#include <iostream>
#include <cmath>
#include <cstdlib>
#include <stdio.h>
#include <vector>
using namespace std;
#define rep(i,n) for(int i=0;i<n;i++)

int N;
vector<int> A(N);

int main() {
    int a,b;
    cin >> a >> b;
    int rm = -1;
    while(rm != 0) {
        rm = a % b;
        a = b;
        b = rm;
    }
    cout << a << endl;
}
