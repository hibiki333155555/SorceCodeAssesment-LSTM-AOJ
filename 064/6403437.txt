[...a]=require('fs').readFileSync(0,'utf8').split`
`[1].split` `.map(x=>+x);M=Math;console.log([M.min(...a),M.max(...a),a.reduce((y,z)=>y+z)].join` `);

//M=Math;console.log([M.min(...a),M.max(...a),a.reduce((y,z)=>y+z)].join` `);
with(Math)console.log([min(...a),max(...a),a.reduce((y,z)=>y+z)].join` `);
