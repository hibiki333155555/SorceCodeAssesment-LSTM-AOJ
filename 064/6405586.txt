#include<stdio.h>

int main(void)
{
    int n,x;
    scanf("%d\n",&n);
    
    int i=1;
    while(i<=n)
    {
        x=i;
        if ( x % 3 == 0 )
        {
            printf(" %d",i);
            goto END_CHECK_NUM;
        }
        INCLUDE3:
        if ( x % 10 == 3 )
        {
            printf(" %d",i);
            goto END_CHECK_NUM;
        }
        x /= 10;
        if ( x ) goto INCLUDE3;
        END_CHECK_NUM:
        i++;
    }
    printf("\n");
}
