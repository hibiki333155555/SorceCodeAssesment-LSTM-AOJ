#include <bits/stdc++.h>

#define ll long long
#define ld long double
#define rep(i,a,b) for (int i = a; i < b; i++)

using namespace std;

int main(){
    int H,W;
    string s1,s2;   
    while (true){
        cin >> H >> W;
        if (H==0 && W == 0) break;

        s1 = "";
        rep (i,0,W) s1 += "#";
        cout << s1 << endl;

        s2 = "#";
        rep(i,0,W-2) s2 += ".";
        s2 += "#";
        rep(i,0,H-2) cout << s2 << endl;

        cout << s1 << endl;

        cout << endl;
    }
    //system("pause");
    return 0;
}
