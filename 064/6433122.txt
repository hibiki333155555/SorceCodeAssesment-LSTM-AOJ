#include <bits/stdc++.h>
#include <vector>
using namespace std;

int main() {
    
    int x,y;
    for(int i=0; i<30000; i++) {
        cin >> x >> y;
        if(x==0 && y==0) {
            break;
        }
        cout << y << " " << x << endl;
    }
    return 0;
    
}
