#include<stdio.h>
#include<string.h>

int main(void)
{
    char s[101],p[101];
    scanf("%s %s",s,p);
    strcat(s,s);
    if(strstr(s,p)==NULL)printf("No\n");
    else printf("Yes\n");
    return 0;
}
