H = int(input())
heap = [int(x) for x in input()]

for index,i in enumerate(heap):
    print(f'node {index}: key = {i}')
    if 2*index < H:
        print(f', left key = {head[2*index]}')
    if 2*index + 1 < H:
        print(f',right key = {head[2*index + 1]}')
    print()
