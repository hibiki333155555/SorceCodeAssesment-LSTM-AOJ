#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin >> n;
    vector<int> v(n);
    for (int i = 0; i < n; i++) {
        int a;
        cin >> v.at(n-1-i);
    }
    for (int i = 0; i < n; i++) {
        cout << v.at(i);
    }
}
