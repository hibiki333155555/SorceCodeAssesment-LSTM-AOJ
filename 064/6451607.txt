r,c = gets.split.map(&:to_i)
arr = r.times.map{gets.split.map(&:to_i)}
o_arr = Array.new(c, 0)
arr.each do |n|
    puts n.push(n.sum).join(" ")
    c.times do |k|
        o_arr[k] += n[k]
    end
end
puts o_arr.push(o_arr.sum).join(" ")
