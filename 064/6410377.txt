#include<stdio.h>
#include<math.h>
int main(){
    double a,b,C,S,L,h;
    double pi=acos(-1);
    
    scanf("%lf %lf %lf",&a,&b,&C);
    C=C*pi/180;
    
    S=0.5*a*b*sin(C);
    L=sqrt(a*a+b*b-2*a*b*cos(C));
    h=S*2/a;
    
    prinf("%f\n%f\n%f\n",S,L,h);
    
    return 0;
}
