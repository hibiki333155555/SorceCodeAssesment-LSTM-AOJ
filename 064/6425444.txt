#include <iostream>
#include <algorithm>
#include <iomanip>
#include <climits>
#include <string>
#include <cmath>

using namespace std;

int main()
{
int n;
cin >> n;
int minN = INT_MIN, maxN=INT_MAX
long long sum = 0;
for (int i = 0; i < n; i ++)
  {
    int x; cin >> x;
    minN = min (minN, x);
    maxN = max (maxN, x);
    sum += x;
  }
  }
