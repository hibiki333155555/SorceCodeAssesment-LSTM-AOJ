#include<stdio.h>
#include<string.h>
int main()   {
    int k, n, s, j;
    char ca[201], car[201];
    while (1) {
        scanf("%s\n%d", ca, &n);
        if (!strcmp(ca, "-")) break;
        for ( int i=0; i<n; i++ ) {
            scanf("%d", &s);
            for ( j=s; ca[j]!="\0"; j++ )  
                car[j-s] = ca[j];
                for ( k=0; k<s; k++ )
                car[j-s+k] = ca[k];
            for (j=0; ca[j]!="\0"; j++) {
                ca[j] = car[j];
                }    
         printf("%s\n", ca);
            }
        }
    }
}
