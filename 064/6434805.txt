#include <stdio.h>

int G[100000][100000];
int visited[100000];

int ok;

int main(void) {
  int n, m;
  scanf("%d %d", &n, &m);
  int a, b, k = 0, tmp = -1;
  for (int i = 0; i < m; i++) {
    for (int k = 0; k < m; k++) {
      G[i][k] = -1;
    }
  }

  for (int i = 0; i < m; i++) {
    scanf("%d %d", &a, &b);
    if (tmp != a) {
      k = 0;
    }
    tmp = a;
    G[a][k++] = b;
  }
}

