#include<stdio.h>
#include<math.h>
int main()  {
    double distance;
    int x1, y1;
    int x2, y2;
    scanf("%d %d", &x1, &y1);
    scanf("%d %d", &x2, &y2);
    distance = pow(((pow(x1 - x2), 2) + pow((y1 - y2, 2)), 0.5));
    printf("%lf", distance);
    return 0;
}

