#include <bits/stdc++.h>
//#include<atcoder/all>
//using namespace atcoder;
using namespace std;
using ll = long long;
#define all(A) A.begin(),A.end()
using vll = vector<ll>;
using vvll = vector<vll>;
#define rep(i, n) for (long long i = 0; i < (long long)(n); i++)



int main() {
    while(1){
        string C;
        getline(cin,C);
        if(C=="END OF INPUT")return 0;
        ll a=0;
        rep(i,C.size()){
            if(C[i]==' '){cout<<a;a=0;}
            else a++;
        }
        cout<<a<<endl;
    }
}
