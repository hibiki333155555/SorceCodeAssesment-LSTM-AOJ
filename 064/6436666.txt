#include <bits/stdc++.h>
#define lol long long
using namespace std;
int main(){
    int a[2];
    while(true){
        cin>>a[0]>>a[1];
        if(a[0] == 0 && a[1] == 0){
            return 0;
        }
        sort(a,a+2);
        cout<<a[0]<<" "<<a[1]<<"\n";
    }
    return 0;
}
