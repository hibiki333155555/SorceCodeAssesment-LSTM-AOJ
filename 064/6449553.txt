#include <bits/stdc++.h>
using namespace std;
#define rep(i,n) for(int i = 0; i < (int)n; i++)
using ll = long long;

int calc(string s) {
  if(s.size() == 1) return (int)(s[0]-'A');
  return 26+(int)(s[0]-'A')*26+(int)(s[1]-'A');
}

int main(){
  string c1, c2; int r1, r2;
  cin >> c1 >> r1 >> c2 >> r2;
  cout << (calc(c2)-calc(c1)+1)*(r2-r1+1) << endl;
  return 0;
}

