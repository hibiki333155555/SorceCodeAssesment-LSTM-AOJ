const fs=require("fs");

const Main=function(input){
    input=input.split(" ");
    var val=input[0]*input[1];
    var lemgth=2*(input[0]+input[1]);
    console.log(val+" "+length);
}

Main(fs.readFileSync("/dev/stdin","utf8"));
