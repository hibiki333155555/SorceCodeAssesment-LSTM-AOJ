#include <bits/stdc++.h>
using namespace std;
int main() {
   int n,k,p;
   cin>>n>>k;
   int a[n];
   for(int i=0;i<n;i++)
   {
       cin>>a[i];
   }
//cin>>k;
   deque<int>dq;
   for(int i=0;i<k;i++)
   {
       if(dq.empty())
       {
           dq.push_back(i);
       }
       else
       {
           while(!dq.empty() &&(a[dq.back()]>a[i]))
           {
               dq.pop_back();
           }
           dq.push_back(i);
       }
   }
       for(int i=k;i<n;i++)
       {
           cout<<a[dq.front()]<<" ";
           if(!dq.empty() &&(dq.front()<=i-k))dq.pop_front();
          while(!dq.empty() &&(a[dq.back()]>a[i]))
          {
              dq.pop_back();
          }
          dq.push_back(i);
       }
       if(!dq.empty())
       {
           cout<<a[dq.front()];
       }

	return 0;
}
