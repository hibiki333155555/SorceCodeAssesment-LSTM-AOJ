#include <bits/stdc++.h>

using namespace std;

int main()
{
    int m,n,t;
    cin >> m >> n;
    const int MOD = 1e9 + 7;
    if (n == 0) cout << 1;
    t = pow(m,n/2);
    t = (t*t) % MOD;
    if (n % 2 == 1) t = t * m % MOD;
    cout << t << endl;
    return 0;
}

