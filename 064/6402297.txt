#include <iostream>
#include <string>

using namespace std;

int main(){
    string key, str;
    getline(cin, key);
    
    int cnt= 0;
    while(getline(cin, str)){
        cnt += str.count(key);
    }
    cout << cnt << endl;
    return 0;
}
