#include <stdio.h>
 int main(void)
 {
     int a0,a1,a2,r;
     
    scanf("%d %d %d",&a0,&a1,&a2);
    if((a1 - a0) && (a2 - a1))
    {
         printf("%d\n",a0 + (a1 - a0) * 9); 
    }
    else
    {
        r = (a1 / a0);
        for(int i = 0; i < 9;i++)
        {
            a0 *= r;
        }
        printf("%d\n",a0);
    }
    return 0;
 }
