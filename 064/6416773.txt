#include <bits/stdc++.h>
using namespace std;

int main() {
  int a,b;
  cin >> a >> b;

  int d,e;
  double f;

  d = a / b;
  e = a % b;
  f = (double)a / (double)b;

  cout << d << " " << e << " " << fixed << setprecision(5) << f << endl;
  
  return 0;
}
