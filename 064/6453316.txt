#include <stdio.h>
int main()
{
    int a,b,c,d=0,i;
    scanf("%d%d%d",&a,&b,&c);
    for(i=1;i<=c;i++)
    {
      if(c%i==0)
      {
        if(a<=i && i<=b)
        {
          d++;
        }
      }
    }
    printf("%d",d);
}
