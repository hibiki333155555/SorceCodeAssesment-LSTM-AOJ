#include <bits/stdc++.h>
using namespace std;

int main(){
    double a, b, c;
    cin>>a>>b>>c;
    cout<<fixed<<setprecision(10);
    double S = (a*b*sin(M_PI*c/180))/2;
    double L = sqrt(a*a+b*b-2*a*b*cos(M_PI*c/180))+a+b;
    double h = 2*S/a;
    cout<<S<<endl;
    cout<<L<<endl;
    cout<<h<<endl;
    return 0;
}
