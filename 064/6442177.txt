#include <stdio.h>
#include <math.h>

int main(){
    double x,y,x1,x2,y1,y2;
    double sum,dis;
    
    scanf("%lf %lf %lf %lf",&x1,&y1,&x2,&y2);
    y=y2-y1;
    x=x2-x1;
    sum=pow(x,2)+pow(y,2);
    dis=sqrt(sum);
    printf("%.8lf",dis);
}
