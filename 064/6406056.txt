require('fs').readFileSync(0,'utf8').split('\n').some(l=>(
    [H,W]=l.split(` `).map(n=>+n),!H&&(
    //for(i=0,a=[];i<H;)a[i]=(i++%2?'.#':'#.').repeat(W).substr(0,W);
    console.log([...Array(H)].map((_,i)=>(i%2?'.#':'#.').repeat(W).substr(0,W)).join`
`+`
`))));
