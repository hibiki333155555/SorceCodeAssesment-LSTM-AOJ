#include <iostream>

using namespace std;
typedef long long ll;
int main(){
    ll i,n,m,k,x; cin >> n >> m >> k >> x;
    ll mx = -1;
    for(i=0;i<n;i++){
        ll a; cin >> a; mx = max(mx,a);
    }
    if(mx/m*k>=x) cout << "Yes\n";
    else cout << "No\n";
}
