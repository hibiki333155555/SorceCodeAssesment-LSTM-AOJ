#!/usr/bin/env python
import sys
readline=sys.stdin.readline

A,B=map(int,readline().split())
A=bin(A)[2:]
B=bin(B)[2:]
if len(A)>len(B):
    A,B=B,A
A=A.zfill(len(B))
ans=0
for a,b in zip(A,B):
    if a==b:
        ans+=1
    else:
        break
print(ans)
