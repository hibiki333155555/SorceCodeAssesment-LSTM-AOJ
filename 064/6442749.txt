#include <stdio.h>
int main() {
  int x[10000],i;
  while(1)
  {
     scanf("%d", & x);
     if (x==0) 
         break;
  }
  for(i = 1; i<=10000; i++)
 {
  if (x[i]!=0)
    {
     printf("Case %d: %d\n", i,x[i]);
    }
     else
     break;
  }
  return 0;
} 
