#include <iostream>
#include <iomanip>
#include <bits/stdc++.h>
using namespace std;

int main() {
   int x;
   while(true) {
      cin >> x;
      if(x == 0) break;

      int sum = 0;
      while(x) {
         sum += x % 10;
         x /= 10;
      }

      cout << sum << "\n";
   }
    
}
