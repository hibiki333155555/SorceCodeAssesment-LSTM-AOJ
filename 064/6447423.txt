#include<stdio.h>
int main()
{
    int n,a,b,c;
    scanf("%d",&n);
    while(n--)
    {
    int a,b,c;
    scanf("%d %d %d",&a,&b,&c);
        if((a*a) + (b * b) == (c * c)){
                printf("YES");
        }
        else{
            printf("NO");

        }

    }
}

