#include <iostream>
using namespace std;
int main(){
  cin.tie(nullptr);
  ios::sync_with_stdio(false);
  int n,x;
  cin>>n>>x;
  int ans=0;
  for(int i=0;i<n;i++){
    int m;
    cin>>m;
    if(m>=x) ans++;
  }
  cout<<ans<<endl;
  
  return 0; 
}

