#include <iostream>
using namespace std;

int main()
{
   string s;
   cin >> s;
   for (auto &alpha : s)
   {
      if (alpha == 'A')
         alpha = 'X';
      else if (alpha == 'B')
         alpha = 'Y';
      else if (alpha == 'C')
         alpha = 'Z';
      else
         alpha -= 3;
   }
   cout << s << endl;

   return 0;
}
