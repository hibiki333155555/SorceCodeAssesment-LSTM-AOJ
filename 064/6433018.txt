#include <bits/stdc++.h>
#include <vector>
using namespace std;

int main() {
    
    while(1) {
        int x,y;
        cin >> x >> y;
        if(x==0 && y==0) {
            break;
        }
        else {
            cout << y << " " << x << endl;
        }
    }
    return 0;
    
}
