#include <iostream>
#include <algorithm>
#include <vector>
using namespace std;

int main(){
    int a, b, c;
    cin >> a >> b >> c;
    
    vector<int> num{a,b,c};
    sort(num.bigin(), num.end());
    
    for(int i = 0; i < 3; i++){
        cout << num[i] << " ";
    }
    
    return 0;
}
