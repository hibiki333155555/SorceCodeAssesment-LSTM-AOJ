#include <bits/stdc++.h>
using namespace std;

int main() {
    int a, b, d, r;
    double f;
    cin >> a >> b;
    cout << fixed << setprecision(5);

    cout << a / b << ' ' << a % b << ' '  << (double)a / (double)b << endl;
    
}
