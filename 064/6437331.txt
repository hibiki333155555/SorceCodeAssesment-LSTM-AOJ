#include <iostream>
#include <string>

using namespace std;
int main()
{
    string a;
    cin >> a;

    int x = 0;
    for(int i = 0 ; i < a.size(); i++)
    {
        if(a[i] = '0') break;
        x += a[i] - '0';
    }
    cout << x << endl;
    return 0;
}

