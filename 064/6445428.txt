#include <bits/stdc++.h>
using namespace std;
int main() {
	while (1) {
		int r0, w0, c, r; scanf("%d %d %d %d", &r0, &w0, &c, &r);
		if (!r0 && !w0 && !c && !r) break;

		int ans = 0;
		while (r0 / w0 < c) {
			r0 += r;
			ans++;
		}

		printf("%d\n", ans);
	}
}
