#include<bits/stdc++.h>
using namespace std;
#define rep(i, n) for(int i = 0; i < (n); i++)


int main(){
    //1.Input
    int n;
    cin >> n;
    cout << n << ": ";
    //2.Process
    int m = n;
    for(int i = 2; i * i <= n; i++){
        if(m % i == 0){
            while(m % i == 0){
                m /= i;
                cout << i << " ";
            }
        }
    }
    if(m != 1) cout << m << endl;
    //3.Output
}
