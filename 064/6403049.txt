#include <iostream>
#include <string>
using namespace std;

int main(){
    string x;
    int y;
    while(1){
        cin >> x >> y;
        if(x == "-") break;
        for(int i = 0; i < y; i++){
            int z;
            string a, b, c;
            cin >> z;
            a = x.substr(0, z);
            b = x.erase(0, z);
            x = b + a;
        }
        cout << x << endl;
    }
    
    return 0;
}
