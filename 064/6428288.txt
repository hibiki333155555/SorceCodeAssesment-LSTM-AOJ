#include<stdio.h>

int main()
{
    int x, i, count=1;

    while (1)
    {
    scanf("%d",&x);
        if(x==0) break;

        printf("Case %d: %d\n",count, x);
        count++;
    }

   return 0;
}     
