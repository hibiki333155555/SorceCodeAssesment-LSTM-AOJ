#include <iostream>
#include <vector>
#include <set>
#include <algorithm>
using namespace std;

int main()
{
   int n, m;
   cin >> n >> m;
   vector<int> a(n);
   vector<int> b(m);
   for (int &v : a)
      cin >> v;
   sort(a.begin(), a.end());
   for (int &v : b)
      cin >> v;
   sort(b.begin(), b.end());

   vector<int> result;
   set<int> st;
   set_intersection(a.begin(), a.end(), b.begin(), b.end(), back_inserter(result));

   for (auto &v : result)
      st.insert(v);

   for (auto &s : st)
      cout << s << endl;
   return 0;
}
