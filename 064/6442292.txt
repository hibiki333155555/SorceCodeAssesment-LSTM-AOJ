#include<iostream>
#include<string>
using namespace std;

int main(){
    string T, P; cin >> T >> P;
    int np = P.size(), nt = T.size();
    for(int i=0; i<=nt-np; ++i){
        if(T.substr(i,np) == P) cout << i << endl;
    }
    return 0;
}
