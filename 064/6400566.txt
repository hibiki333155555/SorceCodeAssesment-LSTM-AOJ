#include <iostream>
#include <string>
#include <algorithm>
using namespace std;

int main() {
    string str,order,p;
    int q,a,b;
    cin >> str >> q;
    for (int i=0;i<q;i++) {
        cin >> order;
        if (order == "replace") {
            cin >> a >> b >> p;
            cout << str.replace(a, b, p) << endl;;
        } else if (order == "print") {
            cout << str.substr(a, b-a+1) << endl;
        } else if (order == "reverse") {
            cout << reverse(str.begin(), str.end()) << endl;
        }
    }
}
