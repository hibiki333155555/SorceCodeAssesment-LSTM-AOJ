#include <bits/stdc++.h>
using namespace std;

int main(){
    int n,m;
    cin>> n>>m;
    vector<vector<int>> a(n,vector<int>(m));
    vector<int> b(m);
    for(int i=0; i<n; i++){
        for(int j=0; j<m; j++){
            cin>> a[i][j];
        }   
    }
    for(int i=0; i<m; i++){
        cin>>b[i];
    }
    int sum=0;
    for(int i=0; i<n; i++){
        for(int j=0; j<m; j++){
            sum+=a[i][j]*b[j];
        }
        cout<< sum<<endl;
        sum=0;
    }   
    // int sum=0;
    // for(int i=0; i<n; i++){
    //         int b;
    //         cin>>b;
    //         for(int j=0; j<m; j++){
    //             sum+=a[i][j]*b; 
    //         }
    //     cout<< sum <<endl;
    //     sum=0; 
    // }    
}

