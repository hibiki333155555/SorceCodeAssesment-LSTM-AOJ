#include <stdio.h>
int main(void)
{
    int S,h,m,s;
    do
    {
        scanf("%d",&S);
    }while(!(0<=S&&S<86400));
    for(int i = 0; i <= S;i++)
    {
        h = s / 60 / 60;
        m = s / 60;
    }
    printf("%d:%d:%d\n",h,m,s);
    return 0;
}
