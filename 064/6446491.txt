#include <bits/stdc++.h>
using namespace std;

int main() {
	int seconds;
	cin >> seconds;

	int h, m;
	h = seconds / 3600;
	seconds %= 3600;
	m = seconds / 60;
	seconds %= 60;
	printf("%d:%d:%d\n", h, m, seconds);

	return (0);
}

