#include<stdio.h>

int gcd(int x,int y){
  int z;
  
  z = x % y;
  
  if(z == 0)return y;
  
  else gcd(y,z);
}

int main(){
  int x,y;

  scanf("%d%d",&x,&y);

  printf("%d\n",gcd(x,y));

  return 0;
}

