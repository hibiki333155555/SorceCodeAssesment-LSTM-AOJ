#include <iostream>
#include <stdio.h>
#include <cmath>
#include <iomanip>
#include <stack>
#include <vector>
using namespace std;

int main(void){
    int n, m;
    cin >> n >> m;
    //cout << endl;
    vector< vector<int > > a;
    a.resize(n);
    for(int i=0; i<n; i++) {
        a[i].resize(m);
    }
    //cout << endl;

    int b[m];
    for (int j=0; j<m; j++) {
        cin >> b[j];
    }
    //cout << endl;

    int c[n];
    for (int i=0; i<n; i++) {
        for (int j=0; j<m; j++) {
            c[n] += a[i][j]*b[j];
        }
    }

    for (int i=0; i<n; i++) {
        cout << c[i] << endl;
    }

    return 0;
}
