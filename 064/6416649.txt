#include <stdio.h>

int main(void){
    char x[1001];
    int i,sum=0;
    
  while(1){  
    scanf("%s",x);
    sum=i=0;
    while(x[i] != '\0'){
        sum+=x[i++]-'0';
    }
    if(sum==0){break;}
    printf("%d\n",sum);
  }
}
