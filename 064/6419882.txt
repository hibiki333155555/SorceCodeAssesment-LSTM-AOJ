N = int(input())
S = list(map(int, input().split()))
Q = int(input())
T = list(map(int, input().split()))

answer = 0
for key in T:
	l = 0; r = N
	while r > l + 1:
		m = (l + r) // 2
		if S[m] > key:
			r = m
		else:
			l = m
	if S[l] == key:
		answer += 1

print(answer)

