#include <bits/stdc++.h>

#define ll long long
#define ld long double
#define rep(i,a,b) for (int i = a; i < b; i++)
#define irep(i,a,b) for (int i = a; i > b; i--)

using namespace std;

int main(){
    string s = "Hello World";
    rep(i,0,1000) cout << s << endl;
    // system("pause");
    return 0;
}
