#include <iostream>
using namespace std;
static const int S = 12;
int e[S];

bool cheak(){
    //　選択ソート
    for ( int i = 0; i < S = 1; i++){
        int minj = i;
        for ( int j = i +　；j　< S ; j++)
            if (e[j] < e[minj] ) minj = j;
            swap(e[minj], e[i]);
    }

    for ( int i = 0; i < N; i += 4)
        if ( e[i] != e[i + 3 ) return false;
    return true;
}
