#include <iostream>
using namespace std;
typedef long long ll;

int main()
{
	ll n, m, k, x;
	cin >> n >> m >> k >> x;
	x *= m;
	ll l = 0;
	for(int i = 0; i < n; i++){
		ll a;
		cin >> a;
		l = max(a, l);
	}
	if(l * k >= x) cout << "Yes" << endl;
	else cout << "No" << endl;
}
