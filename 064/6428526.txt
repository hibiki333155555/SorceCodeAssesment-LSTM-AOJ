#include <stdio.h>

int main() {
    int a,b,c,x[3],p,i,ans=0;
    x[3]=c;
    x[2]=b;
    x[1]=a;
    scanf("%d %d %d", &a ,&b,&c);
    for (i=a ; i<=b; i++)
    {
        if (c%i==0)
        {
        ans=ans+1;
        }
    }
    printf("%d",ans);
}
