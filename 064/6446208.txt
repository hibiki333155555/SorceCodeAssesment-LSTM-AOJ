#include<bits/stdc++.h>
using namespace std;
const int x = 1e9 + 7;
int n;
long long a[10], res;
int main(){
    cin >> n;
    for(int i=0; i<n; i++)
        cin >> a[i];
    res=a[0];
    for(int i=1; i<n; i++)
        res=(res*a[i])/__gcd(res, a[i]);

    cout << res << '\n';
}

