#include <iostream>
#include <iomanip>
#include <bits/stdc++.h>
using namespace std;

int check(int x, int n) {
   if(++x <= n) return false;
}

int main() {
   int n, i=1, flag=true;
   cin >> n;

   while(flag) {
      int x = i;

      if(x % 3 ==0) {
         cout << " " << i;
         check(x, n);
      }

      while(x) {
         if(x % 10 == 3) {
            cout << " " << i;
            check(x, n);
         }
         x /= 10;
      }
      check(x, n);
      i++;
   }

}
