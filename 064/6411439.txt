#include <iostream>
#include <set>

using namespace std;

int main(){
    int n, op, e;
    cin>>e;
    set<int>s;
    while(n--){
        cin>>op>>e;
        switch(op){
            case 0:
            set.insert(e);
            cout << s.size() << endl;
            break;
            case 1:
            cout << s.find(e)<< endl;
        }
    }
}
