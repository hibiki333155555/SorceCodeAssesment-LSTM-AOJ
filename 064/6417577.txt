#include<stdio.h>
#include<string.h>
#include<ctype.h>

int main(void)
{
    char t[100],w[100],x[100]={"END_OF_TEXT"};
    int count = 0,i;
    scanf("%s",w);
    while(1)
    {
        scanf("%s",t);
        if(strcmp(t,"END_OF_TEXT")==0)break;
        for(i=0;t[i]!='\0';i++)
            {t[i]=tolower(t[i]);}
        if(strcmp(t,w)==0)count++;
    }
    printf("%d\n",count);
    
    return 0;
}
