#include <iostream>
#include <algorithm>
using namespace std;

int main() {
	int A, B;
	cin >> A >> B;
	if (A > B) {
		swap(A, B);
	}
	int d1 = 0, d2 = 0;
	while ((1 << d1) <= B) d1++;
	while ((1 << d2) <= (A ^ B)) d2++;
	cout << (B != 0 ? d1 - d2 : 1) << endl;
	return 0;
}
