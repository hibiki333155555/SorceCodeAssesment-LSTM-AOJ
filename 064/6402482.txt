#include<bits/stdc++.h>
#define fast ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
#define ll long long int
#define endl "\n"

using namespace std;

int main()
{
    fast;
    ll a,b;
    char c;
    cin>>a>>c>>b;
    if(c=='+')
        cout<<a+b<<endl;
    else if(c=='-')
        cout<<a-b<<endl;
    if(c=='*')
        cout<<a*b<<endl;
    else if(c=='/')
        if(a==0 && b==0)
        {

        }
        else
            cout<<a/b<<endl;
    return 0;
}

