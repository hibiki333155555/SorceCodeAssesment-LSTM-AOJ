#include<stdio.h>
int dic(char c1[], char c2[]) {
    int a=0, i=0;
    while (1)  {
        if (c1[i]<c2[i]) return 1;
        if (c1[i]>c2[i]) return 2;
        if (c1[i] == '\0' && c2[i] == '\0') return 0;
        if (c1[i]=='\0') return 1;
        if (c2[i]=='\0') return 2;
        i++;
    }
     return 0;
}

int main()   {
    int n, p1=0, p2=0, i, a;
    char c1[102], c2[102];
    scanf("%d", &n);
    for ( i=0; i<n; i++ ) {
        scanf("%s", &c1);
        scanf("%s", &c2);
        a = dic(c1, c2);
        if (a==0) {
            p1+=1;
            p2+=1;
} else if (a==1) p2+=3; {
    else p1+=3; 
}
   printf("%d %d\n", p1, p2);
   return 0
        }
}
