#include<stdio.h>

int main(void)
{
    int n,x;
    scanf("%d\n",&n);
    
    int i=1;
    CHECK_NUM:
    x=i;
    if ( x % 3 == 0 )
    {
        printf(" %d",i);
        if ( ++i <= n ) goto CHECK_NUM;
    }
    INCLUDE3:
    if ( x % 10 == 3 )
    {
        printf(" %d",i);
        if ( ++i <= n ) goto CHECK_NUM;
    }
    x /= 10;
    if ( x ) goto INCLUDE3;
    
    if ( ++i <= n ) goto CHECK_NUM;

    printf("\n");
}
