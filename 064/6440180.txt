#include <iostream>
#include <vector>
#include <map>
#include <algorithm>
#include <string>
#include <iomanip>
#include <cmath>
// #include "comp.h"
using namespace std;
using ll = long long;

// #define DEBUG
#define FOR(i, a, b) for (int i = (int)(a); i < (int)(b); ++i)
#define rep(i, n) for (int i = 0; i < (int)(n); ++i)

int main()
{
#ifndef DEBUG
    int h, w;

    cin >> h >> w;

    do
    {
        rep(i, h)
        {
            if (i % 2 == 0)
            {
                rep(j, w)
                {
                    if(j % 2 == 0) cout << "#";
                    else cout << ".";
                }
                cout << endl;
            }
            else
            {
                rep(j, w)
                {
                    if(j % 2 == 0) cout << ".";
                    else cout << "#";
                }
                cout << endl;
            }
        }

        cout << endl;
        cin >> h >> w;

    } while (h != 0 || w != 0);

#else
    char op_tmp;

    op_tmp = '?';
    cout << op_tmp;

#endif
}

