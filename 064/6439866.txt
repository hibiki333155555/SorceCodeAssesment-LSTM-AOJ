#include <iostream>
#include <vector>
#include <map>
#include <algorithm>
#include <string>
#include <iomanip>
#include <cmath>
// #include "comp.h"
using namespace std;
using ll = long long;

// #define DEBUG
#define FOR(i, a, b) for (int i = (int)(a); i < (int)(b); ++i)
#define rep(i, n) for (int i = 0; i < (int)(n); ++i)

void call(int n)
{
#ifdef DEBUG
    int i = 1;
CHECK_NUM:
    int x = i;
    if (x % 3 == 0)
    {
        cout << " " << i;
        goto END_CHECK_NUM;
    }
INCLUDE3:
    if (x % 10 == 3)
    {
        cout << " " << i;
        goto END_CHECK_NUM;
    }
    x /= 10;
    if (x)
        goto INCLUDE3;
END_CHECK_NUM:
    if (++i <= n)
        goto CHECK_NUM;

    cout << endl;
#else
    // -------------------
    int i = 1;
    int x;
    do
    {
        x = i;
        if (x % 3 == 0)
        {
            cout << " " << i;
        }
        else
        {
            do
            {
                if (x % 10 == 3)
                {
                    cout << " " << i;
                    break;
                }
                x /= 10;

            } while (x);
        }

    } while (++i <= n);

    cout << endl;

#endif
}

int main()
{
#ifndef DEBUG
    int n;
    cin >> n;
    call(n);

#else
    char op_tmp;

    op_tmp = '?';
    cout << op_tmp;

#endif
}

