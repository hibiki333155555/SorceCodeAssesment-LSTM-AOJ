// ITP1_5_A:   Print a Rectangle
#include <stdio.h>
#include <ctype.h>
#include <stdlib.h>

int main()
{
	int H, W;
	while (1) {
		scanf("%d%d", &H, &W);
		if (H == 0 && W == 0) break;
		for (int h = 0; h < H; h++) {
			for (int w = 0; w < W; w++) putchar('#');
			putchar('\n');
		}
		putchar('\n');
	}
	return 0;
}


/*
#include <stdio.h>
int main(){
    int h, w;
    int i, j;
    while(1){
        scanf("%d%d", &h, &w);
        if (h==0 && w==0)break;
        for(j=0; j < w;j++){
            for(i=0; i < h; i++) putchar('#');
            putchar('\n');
        }
        putchar('\n');
    }
    return 0;
}
*/
