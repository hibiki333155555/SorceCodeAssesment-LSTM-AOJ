#include<stdio.h>
int main(void)
{
    int i,a[10001];
    for (i = 0 ; i < 10000;i++)
    {
        scanf("%d",&a[i]);
    }
    i = 0;
    while(a[i]!='\0')
    {
        printf("Case %d: %d\n",i,a[i]);
        i++;
    }
    return 0;
}
