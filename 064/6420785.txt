#include <bits/stdc++.h>

#define rep(i, n) for(int i = 0; i < (n); i++)
#define all(x) x.begin(), x.end()

using ll = long long;
using namespace std;

int main() {
    int n; cin >> n;
    ll x; cin >> x;
    vector<int> m(n); rep(i, n) cin >> m[i];
    int ans = 0;
    rep(i, n) {
        if (m[i] >= x) ans++;
    }
    cout << ans << "\n";
}
