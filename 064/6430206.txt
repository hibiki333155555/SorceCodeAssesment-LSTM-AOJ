#include <bits/stdc++.h>
using namespace std;
 
int main() {
  int a, b, c;
  cin >> a >> b >> c;
  
  //cの約数を求める
  int count = 0;
  
  for (int i=1; i <= c; i++){
    if (c % i == 0){
      if (i >= a && i <= b){
        count++;
      }
    }
  }
  
  cout << count << endl;
  
}

