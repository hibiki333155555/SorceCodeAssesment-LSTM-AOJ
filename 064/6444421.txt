#include <iostream>
#include <iomanip>
using namespace std;

int main()
{
    int a, b;
    int d, r;
    double f;
    
    d = a / b;
    r = a % b;
    f = (double)a / (double)b;
    
    cout << d << " " << r << " " << fixed << setprecision(6) << f << endl;
    return 0;
}
