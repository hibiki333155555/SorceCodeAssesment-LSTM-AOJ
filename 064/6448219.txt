#include <cstdio>

int main(){
    
    int a=1,x;
    
    while(1){
        
        scanf("%d", &x);
        
        if(x==0) break;
        
        printf("Case %d: %d\n", a, x);
        
        a++;
    }
}
