#include <bits/stdc++.h>
using namespace std;
void Print(vector<int> &v){
	int a;
	for(int i=0;i<v.size();i++){
		cout<<v[i];
		if(i==v.size()-1)
		cout<<"\n";
		else
		cout<<" ";
		
	}
}
void BubbleSort(vector<int> &arr){
	int n=arr.size();
	for(int i=0;i<n-1;i++){
		for(int j=0;j<n-1;j++){
			if(arr[j]<arr[j+1]){
				swap(arr[j],arr[j+1]);
				count++;
			}
			
		}
		
	}
	Print(arr);
	cout<<count;
}
int main(){
	int n,count;cin>>n;
	vector<int> v(n);
	for(int i=0;i<n;i++){
		cin>>v[i];
	}
	BubbleSort(v);
	cout<<"\n";
	return 0;
}
