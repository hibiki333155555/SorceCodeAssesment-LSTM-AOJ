#include<stdio.h>
int main ()
{
    int x,y,W,H,r;
    scanf("%d %d %d %d %d",&W,&H,&x,&y,&r);

    if(x>0 && y>0 && x<W && y<H && 2*r<W && 2*r<H)
        printf("Yes\n");
    else
        printf("No\n");
    return 0;
}

