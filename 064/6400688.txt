#include <cstdio>

int main(void){
	while(1){
		int H,W; 
		scanf("%d %d",&H,&W);

		if( H == 0 && W == 0)	
		    break;

		for (int i = 0; i < H; ++i){
			for (int j = 0; j < W; ++j){
				if((i+j)%2)
					putchar('.');
				else
					putchar('#');
			}
			putchar('\n');
		}
		putchar('\n');
	}
}

