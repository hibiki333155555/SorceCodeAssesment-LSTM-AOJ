#include <algorithm>
#include <bits/stdc++.h>
#include <cmath>
using namespace std;
using ll = long long;
template <typename T> using MaxHeap = priority_queue<T>;
template <typename T> using MinHeap = priority_queue<T, vector<T>, greater<T>>;
#define rep(i, n) for (ll i = 0; i < (ll)(n); i++)
#define ff first
#define ss second
#define pii pair<int, int>
#define pll pair<ll, ll>
#define all(a) a.begin(), a.end()

const ll INF = 1e18;
const ll mod = 998244353;

int main() {
    ll n;
    cin>>n;
    vector<vector<ll>>a(n,vector<ll>(n));
    map<ll,pll>mp;
    rep(i,n){
        rep(j,n){
            cin>>a[i][j];
            if(mp.count(a[i][j])){
                if(mp[a[i][j]].ff >= i && mp[a[i][j]].ss > j){
                    mp[a[i][j]] = {i,j};
                }
            }else{
                mp[a[i][j]] = {i,j};
            }
        }
    }
    ll q;
    cin>>q;
    rep(k,q){
        ll t;
        cin>>t;
        if(!mp.count(t)){
            cout<<-1<<endl;
        }
        else {
            cout<<mp[t].ff+1<<" "<<mp[t].ss+1<<endl;
        }
    }
}

