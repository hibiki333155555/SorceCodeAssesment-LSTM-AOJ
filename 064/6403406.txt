#include<stdio.h>
#include<vector>
#include<iostream>
#include<algorithm>
#include<cmath>
#include<set>
#include<map>
using namespace std;

int main(){
  int n;
  scanf("%d",&n);
  set<string> set;
  char order[100];
  char moji[100];
  for (int i = 0; i < n; i++){
    scanf("%s",order);
    string order2 = order;
    scanf("%s",moji);
    string moji2 = moji;
    if(order2=="insert"){
      set.insert(moji2);
    }
    else if(order2=="find"){
      if(set.count(moji2)){
        printf("%s\n","yes");
      }
      else{
        printf("%s\n","no");
      }
    }  
  }
}
  
