#include<bits/stdc++.h>
using namespace std;
int main()
{
    string s,x;
    cin >> s >> x;
    int i,c,j=0;
    for(i=0;i<s.size();i++)
        if(s[i]==x[j])
            j++;
    if(j<x.size())
    for(i=0;i<s.size();i++)
        if(s[i]==x[j])
            j++;
        else
            break;
    if(j==x.size())
        cout << "Yes\n";
    else
        cout << "No\n";
}

