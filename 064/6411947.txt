#include<bits/stdc++.h>
using namespace std;

int main(){
    double a,b;
    int C;
    cin>>a>>b>>C;
    double PI=acos(-1),rad=PI*C/180;
    printf("%.8f\n%.8f\n%.8f\n",(a*b*sin(rad))/2,a+b+sqrt(pow(a,2)+pow(b,2)-2*a*b*cos(rad)),b*sin(rad));
}
