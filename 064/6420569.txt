#include <stdio.h>

int main()
{
	int i, N, X, M, ans = 0;
	scanf("%d %d", &N, &X);
	for (i = 1; i <= N; i++) {
		scanf("%d", &M);
		if (M >= X) ans++;
	}
	printf("%d\n", ans);
	fflush(stdout);
	return 0;
}
