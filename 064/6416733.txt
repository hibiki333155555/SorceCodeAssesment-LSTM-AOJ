#include <bits/stdc++.h>
using namespace std;

int main() {
  int a,b,c;
  cin >> a >> b >> c;

  int counter = 0;

  for(a;a <= b;a++){
    if(c % a == 0){
      counter++;
    }
  }

  cout << counter << endl;
  
  return 0;
}
