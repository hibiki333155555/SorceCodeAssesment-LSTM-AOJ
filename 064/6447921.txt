#include <iostream>
#include <set>
using namespace std;

int main(){
    int n, q;
    cin >> n;
    set<int> s;
    while(n--){
        int x;
        cin >> x;
        s.insert(x);
    }
    cin >> q;
    // while(q--){
    //     int x;
    //     cin >> x;
    //     t.insert(x);
    // }
    int cnt=0, x;
    while(q--){
        cin >> x;
        cnt += s.count(x);
    }
    // for(int x:t){
    //     // cout << x << endl;
    //     if(s.count(x)) cnt++;
    // }
    cout << cnt;
    return 0;
}
