#include<stdio.h>
int main()
{
    int a,b,c;
    scanf("%d%d%d",&a,&b,&c);
    if(a<=10000 && b<=10000 && c<=10000 && a>=1 && b>=1 && c>=1)
    {
        if(a>b && a>c && b>c)
        {
            printf("%d %d %d\n",c,b,a);
        }
        else if(b>a && b>c && c>a)
        {
            printf("%d %d %d\n",a,c,b);
        }
        else if(c>a && c>b && b>a)
        {
            printf("%d %d %d\n",a,b,c);
        }
        else if(b>a && b>c && a>c)
        {
            printf("%d %d %d\n",c,a,b);
        }
        else if(a>c && a>b && c>b)
        {
            printf("%d %d %d\n",b,c,a);
        }
        else if(c>a && c>b && a>b)
        {
            printf("%d %d %d\n",b,a,c);
        }
        else
        {
            return 0;
        }
    }
    else
    {
        return 0;
    }
    return 0;
}
