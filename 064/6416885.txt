#include<bits/stdc++.h>
using namespace std;
    
int main()
{
    string T,P;
    cin >> T >> P;
    if(T.size() >= T.size()){
        for(int i = 0; i <= T.size() - P.size(); i++){   
        if(P == T.substr(i, P.size())){
          printf("d\n", i);
        }
      }
    }
    
    return 0;
}
