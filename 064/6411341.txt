#include <stdio.h>
int main()
{
    int a,b,c,d,r;
    scanf("%d %d %d %d %d",&a,&b,&c,&d,&r);
   if(c-r >= 0 && c+r<=a && d-r >=0 && d+r <=b)
   {
       printf("Yes\n");
   }
   else 
   {printf("No\n");          // dipu
   }
   return 0;


}
