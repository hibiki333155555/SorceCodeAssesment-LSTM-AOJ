#include <bits/stdc++.h>
using namespace std;

int main()
{
    string T, P;
    cin >> T >> P;
    int tLen = T.length();
    int pLen = P.length();

    for( int i=0; i<tLen-1; i++ )
    {
        if(
           T.substr( i, pLen ) == P &&
           i < tLen-1
        ){
            cout << i << "\n";
        }
    }
    return 0;
}
