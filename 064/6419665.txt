#include <bits/stdc++.h>
using namespace std;

void call(int n) {
  cout << " ";
    for (int i = 1; i < n+1; i++) {
        if (i % 3 == 0) cout << i << ' ';
        else if (i % 10 == 3) cout << i << " ";
    }
}

int main() {
    int n;
    cin >> n;
    call(n);
    cout << " ";
}
