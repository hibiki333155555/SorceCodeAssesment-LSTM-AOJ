#pragma GCC optimize("Ofast")
#include <bits/stdc++.h>
using namespace std;

#define REP(i,n) for(int i=0;i<(n);i++)
#define ALL(v) v.begin(),v.end()
#define debug(a) cerr<<#a<<":"<<a<<endl;
#define debug2(a,b) cerr<<"("<<#a<<","<<#b<<"):("<<a<<","<<b<<")"<<endl;
#define debug3(a,b,c) cerr<<"("<<#a<<","<<#b<<","<<#c<<"):("<<a<<","<<b<<","<<c<<")"<<endl;
#define debug4(a,b,c,d) cerr<<"("<<#a<<","<<#b<<","<<#c<<","<<d<<"):("<<a<<","<<b<<","<<c<<","<<d<<")"<<endl;

template<typename T>
istream& operator>>(istream&is,vector<T>&v){
  for(T&p:v)is>>p;
  return is;
}
template<typename T>
ostream& operator<<(ostream&os,const vector<T>&v){
  if(&os==&cerr)os<<"[";
  for(int i=0;i<v.size();i++){
    os<<v[i];
    if(i+1<v.size())os<<(&os==&cerr?",":" ");
  }
  if(&os==&cerr)os<<"]";
  return os;
}

int main(){
  ios::sync_with_stdio(false);
  cin.tie(nullptr);
  int m;cin>>m;
  vector<bool> s1(26,false),s2(26,false);
  vector<vector<bool>> t1(m,vector<bool>(26,false)),t2(m,vector<bool>(26,false));
  string s;cin>>s;
  for(char p:s)s1[p-'a']=true;
  cin>>s;
  for(char p:s)s2[p-'a']=true;
  REP(i,m){
    cin>>s;
    for(char p:s)t1[i][p-'a']=true;
    cin>>s;
    for(char p:s)t2[i][p-'a']=true;
  }
  REP(i,26)REP(j,26){
    bool ok=true;
    if(!s1[i]||!s2[j])ok=false;
    REP(k,m)if(t1[k][i]&&t2[k][j])ok=false;
    if(ok){
      cout<<"Yes"<<endl;
      cout<<char('a'+i)<<char('a'+j)<<endl;
      return 0;
    }
  }
  cout<<"No"<<endl;
}
