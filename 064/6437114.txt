#include <iostream>
#include <vector>
// #include "comp.h"
using namespace std;

#define FOR(i, a, b) for (int i = (int)(a); i < (int)(b); ++i)
#define rep(i, n) for (int i = 0; i < (int)(n); ++i)

int main()
{
    int x, y;

    do{
        cin >> x >> y ;
        if (x > y) swap(x, y);
        cout << x << " " << y << endl; 
    }while(x == 0 && y == 0);
}

