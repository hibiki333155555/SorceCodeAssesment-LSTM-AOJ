#include <stdio.h>
#include <bits/stdc++.h>
using namespace std;
#define rep(i,n) for(int i=0;i<(n);i++)
#define Inf 1000000001

int main(){
	
	long long N,M,K,X;
	cin>>N>>M>>K>>X;
	
	vector<long long> a(N);
	rep(i,N)cin>>a[i];
	sort(a.begin(),a.end());
	
	if(a.back() * K >= X * M)cout<<"Yes"<<endl;
	else cout<<"No"<<endl;
	
	return 0;
}
