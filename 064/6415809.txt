package main

import (
    "fmt"
)

func main() {
    var x, y int
    fmt.Scanf("%d %d\n", &x, &y)
    if x < y {
        fmt.Printf("a < b\n")
    } else if y < x {
        fmt.Printf("a > b\n")
    } else {
        fmt.Printf("a == b\n")
    }
}

