#include<bits/stdc++.h>

using namespace std;

int main(){
    int num[12];
    for(int i=0;i<n;i++){
        cin >> num[i];
    }
    sort(num,num+12);
    if(num[0] == num[3] && num[4] == num[7] && num[8] == num[11]){
        cout << "yes" << endl;
    }else{
        cout << "no" << endl;
    }
    return(0);
}
