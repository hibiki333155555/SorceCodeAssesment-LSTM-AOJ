#include<stdio.h>

int main(void)
{
    int n;
    long x;
    long y=1000000;
    long z=-1000000;
    long long sum=0;
    
    scanf("%d\n",&n);
    
    for(int i=0;i<n;i++)
    {
        scanf("%d",&x);
        
        if(x<y)
        {
            y=x;
        }
        if(x>z)
        {
            z=x;
        }
        sum=sum+x;
    }
    
    printf("%d %d %11d\n",y,z,sum);
    
    return 0;
    
}
