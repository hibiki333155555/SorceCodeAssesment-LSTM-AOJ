#include<bits/stdc++.h>
using namespace std;
#define rep(i, n) for(int i = 0; i < (n); i++)

int main(){
    //1.Input    
    //2.Process
    int p = 0, q = 0;
    rep(i,4){
        int x;
        cin >> x;
        p += x;
    }
    rep(i,4){
        int x;
        cin >> x;
        q += x;
    }
    //3.Output
    cout << max(p,q) << endl;
}
