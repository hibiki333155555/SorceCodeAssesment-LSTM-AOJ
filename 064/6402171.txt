#include <bits/stdc++.h>
using namespace std;

int main(){
  int n;
  cin >> n;
  vector<long long> fibo(max(2,n+1));
  fibo.at(0)=1;
  fibo.at(1)=1;
  
  for(int i=2; i<=n; i++){
    fibo.at(i) = fibo.at(i-1)+fibo.at(i-2);
  }
  
  cout << fibo.at(n) << endl;
  return 0;
}
