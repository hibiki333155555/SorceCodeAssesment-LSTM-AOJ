#include <iostream>
using namespace std;

int main() {
    float a, b;
    cin >> a >> b;
    int d = a / b;
    int r = (int)a % (int)b;
    float f = a / b;
    printf("%d %d %f", d, r, f);
    return 0;
}
