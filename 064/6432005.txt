#include <stdio.h>
int main(void){
    unsigned long long int tmp,r,x,y;
    scanf("%llu %llu",&x,&y);
    if(x<y){tmp=x;x=y;y=tmp;}
    while((r = x % y) != 0){
        x = y;
        y = r;
    }
    printf("%llu",y);
}

