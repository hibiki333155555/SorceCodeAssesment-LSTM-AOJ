#include <bits/stdc++.h>
using namespace std;
 
int main() {
    int x;
    cin >> x;
    
    int h, m, s;
    int y;  //変数
    h = x / 3600;
    y = x % 3600;
    
    m = y / 60;
    y = y % 60;
    
    s = y;
    
    cout << h << ":" << m << ":" << s << endl;
}
