#include <bits/stdc++.h> 
using namespace std;
#define inf 2147483647

int main(void) {
  int x, y, r;
  cin >> x >> y;
  if (x < y) {
    swap(x, y);
  }
  while (y) {
    r = x % y;
    x = y;
    y = r;
  }
  cout << x << endl;
}
