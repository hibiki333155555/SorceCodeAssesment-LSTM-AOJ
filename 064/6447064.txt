const x=require("fs").readFileSync("/dev/stdin","utf-8").trim("\n").split("\n");
(inputs =>{
    for(let i=0,len=inputs.length-1;i<len;i++){
        console.log(`Case ${i+1}: ${inputs[i]}`)
    }
})(x);
