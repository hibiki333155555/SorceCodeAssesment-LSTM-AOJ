#include<stdio.h>
int main()

{
    while(1)
    {
        int h,w;
        scanf("%d%d",&h,&w);
        if (h==0 && w==0) break;

          for (int i=1; i<=h; i++)
          {
              for (int k=1; k<=w; k++){
              printf("#");
              }
          printf("\n");

          }
    }
}
