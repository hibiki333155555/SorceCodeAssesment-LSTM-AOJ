#include<bits/stdc++.h>
using namespace std;

int main(){
    int N,C;
    cin>>N>>C;
    int P[C];
    int sum=0;
    for(int i=0;i<C;i++){
        cin>>P[i];
        sum+=P[i];
    }
    int ans=sum/(N+1);
    if(sum%(N+1)!=0){
        ans++;
    }
    cout<<ans<<"\n";
    return(0);
}
