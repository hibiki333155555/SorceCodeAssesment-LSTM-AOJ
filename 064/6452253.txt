const x = '3 4\n4 5\n4 5\n2 2\n255 89\n0 0'.trim('\n').split('\n');
const createStr = (w,h,flg) =>{
    let c = ''
    for(let j=0;j<w;j++){
        c+='#';
    }
    for(let i=0;i<h;i++){
        console.log(`${c}`);
    }
    if(flg == 1){
        console.log('');
    }
}
(inp =>{
    for(let i=0,len=inp.length-1;i<len;i++){
        let arr = inp[i].split(' ').map(Number); 
        let h = arr[0];
        let w = arr[1];
        createStr(h,w,1);
    }
})(x);
