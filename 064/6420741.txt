#include <bits/stdc++.h>

#define rep(i, n) for(int i = 0; i < (n); i++)
#define all(x) x.begin(), x.end()

using ll = long long;
using namespace std;

int main() {
    int l, n; cin >> l >> n;
    rep(i, n) {
        int a; cin >> a;
        l -= a;
        if (l <= 0) {
            cout << i + 1 << "\n";
            return 0;
        }
    }
    if (l <= 0) cout << n << "\n";
    else cout << "No" << "\n";
}
