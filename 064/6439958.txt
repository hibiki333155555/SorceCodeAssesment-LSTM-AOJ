#include <iostream>
#include <string>
#include <iomanip>
#include <cmath>
using namespace std;
int main( )
{
   double r;
   cin >> r;
   double pi = M_PI;
   cout << setprecision(16) << pi * pow(r, 2) << " " 
   << setprecision(16) << 2 * pi * r << endl; 
  
}
