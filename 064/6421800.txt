#include <stdio.h>

int main(void){
    int n;
    scanf("%d", &n);
    
    int a[n];
    int i, x;
    for(i = n-1; i >= 0; i-- ){
        scanf("%d", &x);
        a[n] = x;
    };
    
    int j;
    for(j = 0; j < n; j++){
        if(j) printf(" ");
        printf("%d", a[j]);
    }
}
