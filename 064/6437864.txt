#include <stdio.h>

int main(void) {
	int i, a, b, c, count = 0;
	
	/*入力*/
	scanf("%d %d %d", &a, &b, &c);
	
	/*判定*/
	for (i = a; i <= b; i++) {
		if (c % i == 0) count++;
	}
	
	/*出力*/
	printf("%d\n", count);
	
	return 0;
}
