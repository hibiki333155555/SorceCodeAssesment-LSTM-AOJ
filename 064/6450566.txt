#include<bits/stdc++.h>
//#include<iostream>
//#include<math.h>
//#include<string.h>
//#include<stdio.h>
//#include<cstring>
//#include<stdlib.h>
//#include<string.h>
//#include<ctype.h>
//#include<stdio.h>
using namespace std;
//分析见书《挑战程序设计竞赛2》上P115
int n;

struct dian{
	double x;
	double y;
};

void print(struct dian a)
{
	printf("%.6f %.6f\n",a.x,a.y);
}

void draw(int N,struct dian a,struct dian b)
{
	struct dian s,u,t; 
	if(N==0)
		return ;
	
	s.x=2.0/3*a.x+1.0/3*b.x;
	s.y=2.0/3*a.y+1.0/3*b.y;
	
	t.x=1.0/3*a.x+2.0/3*b.x;
	t.y=1.0/3*a.y+2.0/3*b.y;
	
	u.x=(t.x-s.x)*cos(M_PI/3)-(t.y-s.y)*sin(M_PI/3)+s.x;
	u.y=(t.x-s.x)*sin(M_PI/3)+(t.y-s.y)*cos(M_PI/3)+s.y;
	
	draw(N-1,a,s);
	print(s);
	draw(N-1,s,u);
	print(u);
	draw(N-1,u,t);
	print(t);
	draw(N-1,t,b);
	
}

int main()
{	
	int n; 
	struct dian p1,p2;
	p1.x=0;
	p1.y=0;
	p2.x=100;
	p2.y=0;
	
	cin>>n;
	print(p1);
	draw(n,p1,p2);
	print(p2);
	
return 0;
}

