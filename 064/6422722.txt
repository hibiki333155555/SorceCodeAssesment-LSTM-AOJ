using System;
using System.Linq;

namespace AOJ_ITP1_8_A
{
    internal class Program
    {
        public static void Main()
        {
            Console.WriteLine(string.Concat(Console.ReadLine().Select(c => char.IsUpper(c) ? char.ToLower(c) : char.ToUpper(c))));
        }
    }
}

