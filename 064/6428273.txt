#include <stdio.h>
#include <stdlib.h>

int main()
{ int x,cnt = 1;
   while(1)
    {
      scanf("%d",&x);
      if(x == 0)
        break;
      printf("Case %d: %d",cnt,x);
      cnt++;
   }

    return 0;
}

