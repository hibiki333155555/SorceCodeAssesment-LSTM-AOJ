#include<iostream>
#include<stdio.h>
using namespace std;
int main() {
    char ch;
    while(1) {
        scanf("%c", &ch);
        if(ch == '\n') {
            break;
        }
        if(isalpha(ch)) {
            if(isupper(ch)) {
                printf("%c", tolower(ch));
            } else if(islower(ch)) {
                printf("%c", toupper(ch));
            }
        } else {
            printf("%c", ch);
        }
    }
}
