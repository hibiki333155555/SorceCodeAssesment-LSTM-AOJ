#include<bits/stdc++.h>
 
using namespace std;
 
typedef long long ll;
typedef unsigned long long ull;
typedef long double lld;
 
 
int main()
{
    #ifndef ONLINE_JUDGE
        freopen("Error.txt", "w", stderr);
    #endif
    string w,s,tmp;
    tmp = "";
    s = "";
    cin>>w;
    while(1)
    {
        cin>>tmp;
        s += " ";
        if(tmp == "END_OF_TEXT") break;
        s += tmp;
    }

    for(int i=0; i<(int)w.length(); ++i) w[i] = tolower(w[i]);
    for(int i=0; i<(int)s.length(); ++i) s[i] = tolower(s[i]);
    int ans = 0;
    for(int i=0; i<(int)s.length(); ++i)
    {
        for(int j=0; j<(int)w.length() && i+j<(int)s.length(); ++j)
        {
            if(s[i+j]==w[j])
            {
                if(j==(int)w.length()-1) {++ans; i+=j;}
            }
            else break;
        }

    }
    // cerr<<w<<endl<<s<<endl;
    cout<<ans<<endl;
}
