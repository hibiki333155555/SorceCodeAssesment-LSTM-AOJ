#include <iostream>
using namespace std;

int main() {
    while (true) {
        int x, y;
        cin >> x >> y;
        if (x == y == 0) break;
        if (x > y) {
            swap(x, y);
        }
        cout << x << y << endl;
    }

    return 0;
}
