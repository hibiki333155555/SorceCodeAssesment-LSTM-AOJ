#include <bits/stdc++.h>
using namespace std;
int main(){
  int N, X;
  cin >> N >> X;
  vector<int> M(N);
  for (int i = 0; i < N; i++){
    cin >> M[i];
  }
  int ans = 0;
  for (int i = 0; i < N; i++){
    if (M[i] >= X){
      ans++;
    }
  }
  cout << ans << endl;
}
