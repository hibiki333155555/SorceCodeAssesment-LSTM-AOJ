#include <iostream>
#include <set>
using namespace std;

int main(){
    set<int> s;
    int q, total = 0;
    cin >> q;
    for(int i=0; i<q; i++){
        int choice;
        cin >> choice;
        if(choice == 0){
            int x;
            cin >> x;
            if(!s.count(x)){
                s.insert(x);
                total++;
            }
            cout << total << endl;
        }
        else if(choice == 1){
            int x;
            cin >> x;
            cout << s.count(x) << endl;
        }
        else if(choice == 2){
            int x;
            cin >> x;
            s.erase(s.find(x));
            total--;
        }
    }
    return 0;
}
