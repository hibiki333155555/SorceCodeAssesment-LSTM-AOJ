#include <iostream>
#include <vector>
#include <map>
#include <algorithm>
#include <string>
#include <iomanip>
#include <cmath>
// #include "comp.h"
using namespace std;
using ll = long long;

// #define DEBUG
#define FOR(i, a, b) for (int i = (int)(a); i < (int)(b); ++i)
#define rep(i, n) for (int i = 0; i < (int)(n); ++i)

int main()
{
#ifndef DEBUG
    int h, w;

    cin >> h >> w;

    do{
        rep(i, h){
            rep(j, w){
                cout << "#" ;
            }
            cout << endl;
        }
    }while(h == 0 && w == 0);

#else
    char op_tmp;

    op_tmp = '?';
    cout << op_tmp;

#endif
}

