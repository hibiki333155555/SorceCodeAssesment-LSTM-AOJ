#include<iostream>
using namespace std;

int main() {
    int m, f, b, d;
    cin >> m >> f >> b;

    if(m + f >= b) {
        d = b - m;
        if(d > 0) {
            cout << d << endl;
        } else {
            cout << 0 << endl;
        }
    } else {
        cout << "NA" << endl;
    }
}
