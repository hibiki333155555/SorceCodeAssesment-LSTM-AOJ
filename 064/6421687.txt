#pragma GCC optimize("Ofast")
#include <bits/stdc++.h>
using namespace std;

int v[223456];
stack<int> sta;

int main(){
  ios::sync_with_stdio(false);
  cin.tie(nullptr);
  
  string s;cin>>s;
  for(int i=0;i<n;i++)
    if(s[i]>='0'&&s[i]<='9'){
      v[i]+=s[i]-'0';
      sta.push(s[i]-'0');
    }
    else if(s[i]==')'){
      v[i]-=sta.top();
      sta.pop();
    }
  for(int i=0;i<s.size();i++)
    if(s[i]>='a'&&s[i]<='z')
      cout<<char('a'+(s[i]-'a'+v[i])%26);
  cout<<endl;
}
  
