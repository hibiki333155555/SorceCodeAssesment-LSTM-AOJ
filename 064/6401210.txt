#include<stdio.h>
#include<unistd.h>
int main() {
    char c;
    int ans = 0;
    while(read(0, &c, 1)) {
        if (c==10) printf("%d\n", ans), ans=0;
        else if (!ans&&c == 48) break;
        else ans += c-48;
    }
}
