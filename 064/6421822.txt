#include <bits/stdc++.h>
using namespace std;

int main() {
    
    int N,X;
    cin >> N >> X;
    vector<int> M(N);
    int cnt = 0;
    for(int i=0; i<N; i++) {
        cin >> M[i];
        if(X>=M[i]) {
            cnt++;
        }
    }
    cout << cnt << endl;
    return 0;
    
}
