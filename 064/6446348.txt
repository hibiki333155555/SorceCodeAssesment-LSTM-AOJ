#include <stdio.h>

int main(void){
	int a,b;
	char op,dummy;
	int ans;

	while( 1 ){
		scanf("%d %c %d", &a, &op, &b);

		ans = 9;
		if( op == '?' ){
			break;
		}else if( op =='+' ){
			ans = a+b;
		}else if( op =='-' ){
			ans = a-b;
		}else if( op =='*' ){
			ans = a*b;
		}else if( op =='/' ){
			ans = a/b;
		}
		printf("%d\n", ans);
	}

	return 0;
}
