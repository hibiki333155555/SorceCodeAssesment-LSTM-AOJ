#include <bits/stdc++.h>
using namespace std;

int main()
{
	int s;
	cin >> s;
	int hour = s / 3600;
	int min = (s % 3600) / 60;
	int sec = s % 60;
	cout << hour << ":" << min << ":" << sec << endl; 
}

