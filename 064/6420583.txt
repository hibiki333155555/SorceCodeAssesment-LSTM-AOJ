#include<cstdio>

int main(){
    int L, N;
    scanf("%d %d", &L, &N);
    for (int i = 0; i < N; i++)
    {
        int A;
        scanf("%d", &A);
        L -= A;
        if(L <= 0){
            printf("%d\n", i+1);
            return 0;
        }
    }
    printf("No\n");
}
