const x = require('fs').readFileSync('/dev/stdin', 'utf8');
const arr = x.split(' ').map(Number);
if(arr[0] < arr[1]){
    ans = `a < b`;
}else if(arr[0] > arr[1]){
    ans = `a > b`;
}else if(arr[0] == arr[1]){
    ans = `a == b`;
}
else{
    return -1;
}
console.log(ans);
