#include<bits/stdc++.h>
using namespace std;

#define ll long long
#define vi vector<int>
#define all(v) v.begin(),v.end()
#define fi first
#define se second
#define P pair<int,int>
#define mp(a,b) make_pair(a,b)
#define pb(a) push_back(a)

int mod=1e9+7;
int inf=INT_MAX;


int main(){
    cin.tie(nullptr);
    ios_base::sync_with_stdio(false);

    int n; cin>>n;

    vector<ll> v(n);
    for(int i=0;i<n;i++){
        cin>>v[i];
    }

    cout<<*min_element(all(v))<<" "<<*max_element(all(v))<<" "<<accumulate(all(v),0LL)<<"\n";

    return 0;
}

