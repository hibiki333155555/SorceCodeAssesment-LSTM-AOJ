#include <iostream>
#include <vector>
#include <map>
#include <algorithm>
#include <string>
#include <iomanip>
#include <cmath>
// #include "comp.h"
using namespace std;
using ll = long long;

// #define DEBUG
#define FOR(i, a, b) for (int i = (int)(a); i < (int)(b); ++i)
#define rep(i, n) for (int i = 0; i < (int)(n); ++i)

int main()
{
#ifndef DEBUG
    int n, tmp;
    vector<int> a;

    cin >> n;
    rep(i, n)
    {
        cin >> tmp;
        a.push_back(tmp);
    }

    reverse(a.begin(), a.end());

    rep(i, n - 1) cout << a[i] << " ";

    cout << a[n-1] << endl;

#else
    char op_tmp;

    op_tmp = '?';
    cout << op_tmp;

#endif
}

