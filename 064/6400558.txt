#include <cstdio>
int main(){
	while(1){
		int H,W;
		scanf("%d %d",&H,&W);

		if(H == 0 && W == 0)	
		    break;

		for(int i = 0; i < H; ++i){
			for (int j = 0; j < W; ++j)
				putchar('#');
			putchar('\n');
		}
		putchar('\n');
	}
}

