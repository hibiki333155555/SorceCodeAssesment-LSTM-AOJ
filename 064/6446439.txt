#include <bits/stdc++.h>
using namespace std;
int main()
{
    int x,y,z;
    while (1)
    {
        scanf("%d",&z);
        if (z==0) break;
        y=cbrt(pow(z,3)-1);
        x=cbrt(pow(z,3)-pow(y,3));
        printf("%.0f\n",pow(z,3)-pow(x,3)-pow(y,3));
    }
}
