#include <iostream>
#include <string>
#include <algorithm>
#include <cmath>
using namespace std;
int main( )
{
    int a = 0, b = 0;
    cin >> a >> b;
    if(a == b) cout << "a == b" << endl;
    if(a > b) cout << "a > b" << endl;
    if(a < b)  cout << "a < b" << endl;
  
   
}
