#include<bits/stdc++.h>
using namespace std;

int main()
{
    string str;
    getline(cin,str);
    stringstream is;
    is.str(str);
    string s;
    while(is>>s){
        string tmp=s;
        char k;
        if(s[s.size()-1]>='a'&&s[s.size()-1]<='z'){

        }else{
            tmp=s.substr(0,s.size()-1);
            k=s[s.size()-1];
        }
        if(tmp=="apple"){
            tmp="peach";
        }else if(tmp=="peach"){
            tmp="apple";
        }
        tmp=tmp+k;
        cout<<tmp;
    }
}

