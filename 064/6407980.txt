#include <stdio.h>
#include <ctype.h>
#include <stdlib.h>

int main()
{
	int n, x;
    scanf("%d%d",&n,&x);
    int a[n][x],b[n],c[x];
    for(int i=0; i<x; i++)
    c[i]=0;
    int sum=0;
    for(int i=0; i<n; i++){
        b[i]=0;
        for(int j=0; j<x; j++){
            scanf("%d",&a[i][j]);
            b[i]+=a[i][j];

            c[j]+=a[i][j];
        }           
         sum+=b[i];
    }
    for(int i=0; i<n; i++){
        for(int j=0; j<x; j++){
            printf("%d ",a[i][j]);
        }
            printf("%d\n",b[i]);

    }
    for(int i=0; i<x; i++)
    printf("%d ",c[i]);

    printf("%d",sum);

    

	return 0;
}

