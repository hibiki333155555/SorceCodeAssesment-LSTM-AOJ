#include <cstdio>

const int MAX_n 100;
const MIN_a 0;
const MAX_a 1000;


int main(void){
    int n;
    int a[n];
    int cov;
    
    scanf("%d", &num);
    if(n>MAX_n){
        return 0;
    }
    
    for(int i=0; i<n; i++){
        scanf("%d", &cov);
        if(cov<MIN_a || cov>=MAX_a){
            return 0;
        }
        a[i] = cov;
    }
    for(int i=0; i<n; i++){
        printf("%d ", a[i]);
    }
    return 0;
}
