#include<stdio.h>
#include<string.h>

int main(){
    char target[100],sub1[100],sub2[100];
    int h,i,j,k;
    
    scanf("%s", &target);
    
    scanf("%d", &h);
    int m[h];
    
    for(i=0;i<h;i++){
        scanf("%d", m[i]);
    }
    
    for(i=0,j=m[i];i<h;i++,j++){
        sub1[i]=target[j];
    }
    
    for(i=0,j=0;j<stlen(target)-m[i];i++,j++){
        sub2[j]=target[j];
    }
}
