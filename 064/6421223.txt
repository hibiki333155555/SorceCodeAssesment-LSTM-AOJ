#include <bits/stdc++.h>
#define rep(i,n) for(int i = 0; i < (n); i++)
using namespace std;
typedef long long ll;

int main(){
    cin.tie(0);
    ios::sync_with_stdio(0);
    
    int N,X; cin >> N >> X;
    int ans = 0;
    rep(i,N) {
        int M; cin >> M;
        ans += (M >= X);
    }
    cout << ans << endl;
}
