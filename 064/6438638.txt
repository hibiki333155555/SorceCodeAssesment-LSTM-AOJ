#include<stdio.h>
#include<math.h>
int main() {
    double a, b, C;
    double e;
    double S, L, h;
    scanf("%lf, %lf, %lf", a, b, C);
    e = C*M_PI/2;
    S = b*c/2*sin(C);
    c = sqrt(a*a+b*b-2*a*b*cos(C));
    L = a+b+c;
    h = S*2/a;
    printf("%lf %lf %lf", S, L, h);
