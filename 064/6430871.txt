#include <bits/stdc++.h>
using namespace std;

int main() {
 
    int n,j,i,tem;
    cin >> n;
    vector<int>v(n);
    for(int i=0;i<n;i++)
    {
    	cin >> v[i];
    
    }
    	  for(int i=0;i<=n-1;i++)
	    {
	    	if(i<n-1)
	    	{
	    	cout << v[i] << " ";
	    	}
	    	else if(i==n-1)
	    	{
	    		cout<<v[i];
	    	}
		
		}
    cout <<"\n";
    for(int i=1;i<n;i++)
    {
    j=i-1;
    tem=v[i];
    while(j>=0 && tem < v[j])
     {
      v[j+1]=v[j];
      j--;
     }
    v[j+1]=tem;
   for(int i=0;i<=n-1;i++)
	    {
	    	if(i<n-1)
	    	{
	    	cout << v[i] << " ";
	    	}
	    	else if(i==n-1)
	    	{
	    		cout<<v[i];
	    	}
		
		}
    cout <<"\n";

    }
    
    return 0;
}
