#include <iostream>
#include <vector>
#include <algorithm>
#include <deque>
#include <queue>
#include <string>
#include <iomanip>
#include <set>
#include <unordered_set>
#include <map>
#include <unordered_map>
#include <utility>
#include <stdio.h>
#include <math.h>
#include <assert.h>
#if __has_include(<atcoder/all>)
#include <atcoder/all>
using namespace atcoder;
#endif
using namespace std;
using ll=long long;

int main(){
  string S;
  cin>>S;
  deque<int> dq;
  int now=0;
  string ans;
  ans.reserve(S.size());
  for(int i=0;i<(int)S.size();i++){
    if(S[i]<='z' && S[i]>='a'){
      ans.append(1,(char)('a'+(S[i]-'a'+now)%26));
    }else if(S[i]=='('){

    }else if(S[i]==')'){
      now-=dq.back();
      dq.pop_back();
    }else{
      now+=S[i]-'0';
      dq.push_back(S[i]-'0');
    }
  }
  cout<<ans<<endl;
}


