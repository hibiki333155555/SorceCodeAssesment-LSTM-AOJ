using System;
class program
{
    static void Main(string[] args)
    {
        string[] s = Console.Parse().Split();
        int h = int.Parse(s[0]);
        int w = int.Parse(s[1]);
        for(int i = 0; i <= h; i++)
        {
            Console.WriteLine("#" * w);
        }
    }
}
