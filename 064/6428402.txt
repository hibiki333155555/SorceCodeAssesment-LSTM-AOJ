#include <iostream>
using namespace std;

int main()
{
    int sec;
    cin >> sec;

    int min = sec / 60;
    sec %= 60;

    int hour = min / 60;
    min %= 60; 

    cout << hour << ":" << min << ":" << sec << endl;
    return 0;
}
