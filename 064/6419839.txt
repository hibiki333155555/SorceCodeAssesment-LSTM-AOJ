#include <bits/stdc++.h>
using namespace std;
struct dot{
    long double x, y;
};

long double d2(long double x1, long double x2){
    
    return (x1-x2)*(x1-x2);
}


int main(){
    dot a1, a2;
    cin>>a1.x>>a1.y>>a2.x>>a2.y;
    cout<<sqrt(d2(a1.x, a2.x)+d2(a1.y, a2.y))<<endl;
    return 0;
}
