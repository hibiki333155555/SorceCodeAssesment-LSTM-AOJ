#include <iostream>
#include <stdio.h>
#include <cmath>
#include <string>

#define ll long long

using namespace std;

int main(){
    int m,f,b;
    cin >> m >> f >> b;
    string ans;
    if (m>=b) ans = "0";
    else if (m+f<b) ans = "NA";
    else ans = to_string(b-m);
    cout << ans << endl;
    //system("pause");
    return 0;
}
