#include <bits/stdc++.h>
using namespace std;

int main()
{

    int n, mark=0;
    cin>> n;
    int arr[n+5];
    for(int i=0; i<n; i++) {
        cin>> arr[i];
    }
    int m;
    cin>> m;
    int arr2[m+5];
    for(int i=0; i<m; i++) {
        cin>> arr2[i];
    }

    if(n> m)
    {
        for(int i=0; i<n; i++) {
            if(i== m-1 && arr[i] == arr2[i]) {
                mark= 2;
                break;
            }
            if(arr[i] < arr2[i]) {
                mark= 1;
                break;
            }

            else if(arr[i] > arr2[i]) {
                mark= 2;
                break;
            }

        }
       // cout<<mark<< endl;
    }

    else {
        for(int i=0; i<m; i++) {
            if(i== n-1 && arr[i] == arr2[i]) {
                mark= 1;
                break;
            }
            if(arr[i] < arr2[i]) {
                mark= 1;
                break;
            }

            else if(arr[i] > arr2[i]) {
                mark= 2;
                break;
            }
        }
    }
    if(mark==2)
    mark=0;
    cout<< mark<< endl;

    return 0;
}

