#include<bits/stdc++.h>
using namespace std;
int main(){
    int a,b,x; cin>>a>>b>>x;
    a=min(a,b*2);
    if(x%500!=0) x+=500-(x%500);
    cout<<(x/1000)*a+(x%1000)/500*min(a,b)<<endl;
    return 0;
}
