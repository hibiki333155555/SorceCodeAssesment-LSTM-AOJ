#include <stdio.h>

int main ()
{
    int a,b,c,i;
    scanf("%d,%d,%d",&a,&b,&c);
    int count=0;
    for(i=a,i<=b,i++)
    {if(C%i==0)
    int cnt=0;
    cnt++;
    
    }
    printf {"%d\n",cnt};

return 0;
}
