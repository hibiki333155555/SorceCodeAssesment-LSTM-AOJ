#include <stdio.h>

int main(void) {
	int a, b, d, r;
	double f;
	
	/*入力*/
	scanf("%d %d", &a, &b);
	
	/*出力*/
	d = a / b; r = a % b; f = (double)a / (double)b;
	printf("%d %d %f\n", d, r, f);
	
	return 0;
}
