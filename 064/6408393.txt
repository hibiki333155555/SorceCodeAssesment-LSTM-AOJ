#include <bits/stdc++.h>
#define rep(i, f, n) for (int i = f; i < (int)(n); i++)
using namespace std;

int main(){
  int n,x;
  cin>>n>>x;
  int cont=0;
  rep(i,0,n-2){
     rep(j,i+1,n-1){
        rep(k,j+1,n){
            int val=i+j+k;
            if(val==x) count++;
        }
      }
   }
  cout<<count;
}
