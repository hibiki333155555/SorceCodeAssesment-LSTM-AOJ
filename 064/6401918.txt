let x = require(`fs`).readFileSync(`/dev/stdin`, `utf-8`).split(` `);
let op = (a, b) => {
    if (a > b) return `a > b`;
    else if (a < b) return `a < b`;
    else if (a == b) return `a == b`;
}
console.log(op(+x[0], +x[1]));
