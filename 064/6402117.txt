#include <iostream>

using namespace std;

int gcd(int x, int y)
{
  if (y == 0) return x;
  else return gcd(y, x % y);
}

int main(int argc, char const *argv[])
{
  int x, y;

  cin >> x >> y;

  int z;
  if (x >= y) z = gcd(x, y);
  else z = gcd(y, x);
  
  cout << z << endl;

  return 0;
}

