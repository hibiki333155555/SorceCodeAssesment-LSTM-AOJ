#include<stdio.h>
int main()
{
    int i;
    int n,a,sum=0,min=1000000,max=-1000000;
    scanf("%d",&n);
    for(i=0 ; i<n ; i++)
    {
        scanf("%d",&a);
        sum+=a;
        if(min>a) min=a;
        if(max<a) max=a;
    }
    printf("%d %d %d\n",min,max,sum);
    return 0;
}
