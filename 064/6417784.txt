#include<stdio.h>
#include<string.h>
int main(void)
{
    char cards[201],sub[200];
    int m,h,i,j,k;
    while(1)
    {
        scanf("%s %d",cards,&m);
        if(cards[0]=='-')break;
        for(i=0;i<m;i++)
        {
            scanf("%d",&h);
           
        }
        printf("%s",cards);
    }
    return 0;
}
