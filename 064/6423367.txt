/*
完成
*/

#include <iostream>
#include <vector>

using namespace std;

int Fib(int n)
{
    std::vector<int> fib(n + 2);
    fib[0] = 1;
    fib[1] = 1;
    for (int i = 2; i <= n; i++)
    {
        fib[i] = fib[i - 1] + fib[i - 2];
    }
    return fib[n];
}

int main(void)
{
    int n;
    std::cin >> n;
    std::cout << Fib(n) << std::endl;
    return 0;
}
