const fs=require("fs");

function Main(input){
    input=input.split(" ");
    if(input[0]===input[1]){
        console.log(a+" == "+b);
    }else if(input[0]<input[1]){
        console.log(a+" < "+b);
    }else if(input[0]>input[1]){
        console.log(a+" > "+b);
    }
}

Main(fs.readFileSync("/dev/stdin","utf8"));
