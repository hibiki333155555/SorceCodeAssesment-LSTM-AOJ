#include <iostream>
using namespace std;

int main(){
    int w,h,x,y,r;
    cin >> w >> h >> x >> y >> r;
    if(x-r<=0&&x+r>=w) cout << "No" << endl;
    if(x+r>=0||x-r<w) cout << "Yes" << endl;
    return 0;
}
