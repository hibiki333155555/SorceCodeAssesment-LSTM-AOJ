main()
{
    int S, m, h, s=0;
    
    scanf("%d", &S);
    
    /* 時間に変換 */
    h = S/3600;
    
    /* 分に変換 */
    m = (S-(h*3600))/60;
    
    /* 秒に変換 */
    s = S-(h*3600)+(m*60);
    
    printf("%d:%d:%d\n", h, m, s);
}
