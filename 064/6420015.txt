#include <iostream>
#include <iomanip>
#include <cmath>

using std::cin;
using std::cout; using std::endl;

int main()
{
    double a, b;
    double c;
    cin >> a >> b >> c;
    c = c * M_PI / 180;
    
    double S, L, h;
    
    S = a * b * sin(c) / 2;
    
    double other;
    other = sqrt( a*a + b*b - 2*a*b*cos(c) );
    L = a + b + c;
    
    h = b * sin(c);
    
    cout << std::setprecision(1024)
         << S << L << h
         << endl;
    
    return 0;
}
