#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define all(x) x.begin(),x.end()
#define fol(i,s,g) for(int i=(int)(s); i<(int)(g); i++)
#define fix(ans,k) fixed << setprecision(k) << ans

int main(){
    vector<int> e(4);
    fol(i,0,4) cin>>e[i];
    sort(all(e));
    if(e[0]==e[1]&&e[2]==e[3]) cout<<"yes\n";
    else cout<<"no\n";
    return 0;
}
