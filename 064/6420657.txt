#include <iostream>
using namespace std;

int main(){
  int n, x;
  cin >> n >> x;
  int ans = 0;
  for(int i = 0; i < n; ++i){
    int m;
    cin >> m;
    ans += (m >= x);
  }
  cout << ans << endl;
}

