#include <stdio.h>
#include <vector>
#include <algorithm>
using namespace std;
int main(void)
{
    int a[5],i,flg=1;
    for(int i=0;i<4;i++) scanf("%d",&a[i]);
    sort(a,a+4);
    if(a[0]!=a[1] ||a[2]!=a[3]) flg=0;
    printf("%d\n",flg);
    return 0;
}
