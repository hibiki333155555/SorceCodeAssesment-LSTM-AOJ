#include <iostream>
#include <iomanip>
#include <bits/stdc++.h>
using namespace std;

int main() {
   const double PI=M_PI; 

   double S, L, r;
   cin >> r;

   S = PI * r * r;
   L = 2 * PI * r;

   cout << setprecision(20) << S << " "
       << setprecision(20) << L << "\n";
}
