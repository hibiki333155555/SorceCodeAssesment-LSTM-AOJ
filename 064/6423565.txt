#include <iostream>

using namespace std;

int main()
{
    int  n = 1;
    int  arr[n];
    cin>>n;
    for(int i = 0;i < n;i++)
    {
        cin>>arr[i];
    }
    int a = 0;
    int b = n-1;
    while(a<b)
    {
        int c = arr[a];
        arr[a] = arr[b];
        arr[b] = c;
        a++;
        b--;
    }
    for(int i = 0; i < n;i++)
    {
        if( i != n - 1 )
        {
            cout<<arr[i]<<" ";
        }
        else if(i = n - 1)
        {
        cout<<arr[n - 1];
        }
    }
    return 0;
}

