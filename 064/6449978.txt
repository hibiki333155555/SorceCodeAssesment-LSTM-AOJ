#include <bits/stdc++.h>
using namespace std;
using ll = long long;
using pll = pair<ll, ll>;
#define drep(i, cc, n) for (ll i = (cc); i <= (n); ++i)
#define rep(i, n) drep(i, 0, n - 1)
#define all(a) (a).begin(), (a).end()
#define pb push_back
#define fi first
#define se second

const ll MOD = 1000000007;
const ll MOD2 = 998244353;
const ll INF = 1LL << 60;
const ll N_MAX = 2e5;

int main(){
    string c1, c2;
    ll r1, r2;
    cin >> c1 >> r1 >> c2 >> r2;

    ll c1_num=0, c2_num=0;
    rep(i, (ll)c1.size()){
        c1_num = 26*c1_num + (ll)(c1[i]-'A');
    }

    rep(i, (ll)c2.size()){
        c2_num = 26*c2_num + (ll)(c2[i]-'A');
    }
    
    cout << (r2-r1+1)*(c2_num-c1_num+1) << endl;
}
