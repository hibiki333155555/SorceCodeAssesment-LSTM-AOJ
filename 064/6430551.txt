#include <bits/stdc++.h>
using namespace std;
 
int main() {
  int a, x;
  cin >> a;

  vector<int> vec(a);
  
  for (int i = 0; i < a; i++){
    cin >> vec.at(i); 
  }
  
  int min = *min_element(vec.begin(), vec.end());
  int max = *max_element(vec.begin(), vec.end());
  int sum = 0;
  
  for (int i = 0; i < a; i++){
    sum += vec.at(i); 
  }
  
  cout << min << " " << max << " " << sum << endl;
}

