#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

template<class T> inline bool chmin(T& a, T b) {
    if (a > b) {
        a = b;
        return true;
    }
    return false;
}
template<class T> inline bool chmax(T& a, T b) {
    if (a < b) {
        a = b;
        return true;
    }
    return false;
}

int main(){
    int n, q, si, ti, cnt = 0;
    vector<int> S;

    cin >> n;

    for(int ii1=0; ii1<n; ii1++){
        cin >> si;
        S.push_back(si);
    }

    cin >> q;

    for(int ii1=0; ii1<q; ii1++){
        cin >> ti;
        int bound;
        bound = lower_bound(S.begin(), S.end(), ti) - S.begin();
        if(S[bound] == ti) cnt++;
    }

    cout << cnt << endl;

    return 0;
}
