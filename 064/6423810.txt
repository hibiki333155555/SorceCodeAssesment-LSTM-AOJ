#include<bits/stdc++.h>
#define loop(n) for (int _i=0;_i<(n);_i++)
#define rep(i,n) for (int i=0;i<(n);++i)
#define rrep(i,n) for (int i= (n); i>=0;--i)
#define For(i,r,n) for (int i = (r); i<(n);++i)
#define fore(i,a) for(auto &i:a)
#define all(obj) begin(obj), end(obj)

#define INF 9999
using namespace std;

template<class T>
bool chmin(T &a,const T & b){
    if(b<a){
        a=b;
        return 1;
    }
    return 0;
}

template<class T>
bool chmax(T &a,const T &b){
    if(a<b){
        a=b;
        return 1;
    }
    return 0;
}

int N;
vector<int> honest[15],liar[15];

int main(void){
    
    
    vector<int> pays;
    vector<int> coins={500,100,50,10,5,1};

    while(1){
        int p;
        cin>>p;
        if(p==0)break;

        pays.push_back(1000-p);
    }

    fore(pay,pays){
        int ans=0;
        fore(coin,coins){
            int num=pay/coin;
            ans+=num;
            pay-=coin*num;
            if(pay==0)break;
        }
        cout<<ans<<endl;
    }
    
    return 0;
}
