#include <stdio.h>
#include<math.h>
//#define PI 3.141592653589793238

int main()
{
    int w, h, x, y, r;
    scanf("%d %d %d %d %d",&w, &h, &x, &y, &r);
    if (x <= w && y <= h && r <= (w-x) && r <= (h-y)){
        printf("Yes\n");
    }
    else{
        printf("No\n");
    }

return 0;
}
