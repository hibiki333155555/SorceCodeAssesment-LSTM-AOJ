#include <stdio.h>

int main(void){
    while(1){
        int x, y;
        scanf("%d %d", &x, &y);
        if(x == 0 && y == 0) return 0;
        if(x < y) printf("%d %d\n", x, y);
        else printf("%d %d\n", y, x);
    };
    return 0;
}
