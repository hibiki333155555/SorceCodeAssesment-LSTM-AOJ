#include <bits/stdc++.h>

#define ll long long
#define ld long double
#define rep(i,a,b) for (int i = a; i < b; i++)

using namespace std;

int main(){
    int H,W;
    string s;   
    while (true){
        cin >> H >> W;
        if (H==0 && W == 0) break;
        s = "";
        rep (i,0,W) s += "#";
        rep(i,0,H) cout << s << endl;
        cout << endl;
    }
    //system("pause");
    return 0;
}
