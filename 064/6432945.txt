#include <bits/stdc++.h>
#include <vector>
using namespace std;

int main() {
    
    int W,H,x,y,r;
    cin >> W >> H >> x >> y >> r;
    int X1 = x-r;
    int X2 = x+r;
    int Y1 = y-r;
    int Y2 = y+r;
    if(X1<0 || W<X1) {
        cout << "No" << endl;
        return 0;
    }
    if(X1<0 || W<X1) {
        cout << "No" << endl;
        return 0;
    }
    if(Y1<0 || H<Y1) {
        cout << "No" << endl;
        return 0;
    }
    if(Y2<0 || H<Y2) {
        cout << "No" << endl;
        return 0;
    }
    cout << "Yes" << endl;
    return 0;
    
}
