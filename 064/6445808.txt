#include <iostream>
#include <iomanip>
#include <bits/stdc++.h>
#include <cctype>
using namespace std;

int main() {
   char t;
   cin >> t;

   if(islower(t)) {
      t = toupper(t);
   }else {
      t = tolower(t);
   }

   cout << t << "\n";
}
