#include <bits/stdc++.h>
using namespace std;

int main() {
	int n = 3;
	int arr[ n ];

	for (int i = 0; i < n; i++) {
		cin >> arr[ i ];
	}

	sort(arr, arr + n);

	for (int i = 0; i < n; i++) {
		printf("%d ", arr[ i ]);
	}
	cout << endl;

	return (0);
}

