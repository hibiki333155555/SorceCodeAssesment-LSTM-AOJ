#define BufSize 1024
typedef long long ll;

#include <iostream>
#include <iomanip>
#include <string>
#include <regex>
#include <vector>
#include <deque>
#include <list>
#include <map>
#include <set>
#include <unordered_set>
#include <algorithm>
#include <numeric>
#include <cstdlib>
#include <cmath>
using namespace std;

// const ll mod = 998'244'353;
// const ll mod = 1e9+7;

int main() {
  ll a, b, c, d; cin >> a >> b >> c >> d;
  vector<ll> ratings = {a, b, c, d};
  sort(ratings.begin(), ratings.end());
	ll ans = 1e9;
  do {
    ans = min(ans, abs(ratings[0] + ratings[1] - ratings[2] - ratings[3]));
  } while(next_permutation(ratings.begin(), ratings.end())) ;
  cout << ans << endl;
  return 0;
}

