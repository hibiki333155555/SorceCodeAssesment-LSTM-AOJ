#include <bits/stdc++.h>
using namespace std;
struct node{
    int v,w;
    char t;
    long long d;
    string s;
    bool operator<(node &b) {
        if (v!=b.v)
            return v>b.v;
        else if (w!=b.w)
            return w>b.w;
        else if (t!=b.t)
            return t>b.t;
        else if (d!=b.d)
            return d>b.d;
        else
            return s>b.s;
    }
};
bool cmp(node &a,node &b)
{
    
}
int main()
{
    int n,v,w;
    char t,st[25]={0};
    long long d;
    vector<node>p;
    node temp;
    scanf("%d",&n);
    while (n--)
    {
        scanf("%d %d %c %lld %s",&temp.v,&temp.w,&temp.t,&temp.d,st);
        temp.s=st;
        p.emplace_back(temp);
    }
    sort(p.begin(),p.end());
    for (auto now:p)
        printf("%d %d %c %lld %s\n",now.v,now.w,now.t,now.d,now.s.c_str());
    return 0;
}
