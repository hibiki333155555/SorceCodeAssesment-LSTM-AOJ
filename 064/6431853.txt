#include <bits/stdc++.h>
#define ll long long
using namespace std;
int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);cout.tie(0);
    string s,m;
    cin>>s;
    ll cnt=0;
    label:
        cin>>m;
        if(m==s) {cnt++; }

       if(m=="END_OF_TEXT") { cout<<cnt<<endl; return 0;}
       else {goto label;}

}

