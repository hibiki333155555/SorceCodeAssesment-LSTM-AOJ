a,b,c = map(int,input().split())
divisor = {
    'A':{},
    'B':{},
    'C':{}
}
for i in range(1,10000):
    if a % i ==0:
        A.add(i)
    if b % i ==0:
        B.add(i)
    if c % i ==0:
        C.add(i)
A_C = divisor[A] & divisor[C]
B_C = divisor[B] & divisor[C]
print(len(A_C & B_C))

