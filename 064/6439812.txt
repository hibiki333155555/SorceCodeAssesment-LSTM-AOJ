#include <iostream>
#include <vector>
#include <map>
#include <algorithm>
#include <string>
#include <iomanip>
#include <cmath>
// #include "comp.h"
using namespace std;

// #define DEBUG
#define FOR(i, a, b) for (int i = (int)(a); i < (int)(b); ++i)
#define rep(i, n) for (int i = 0; i < (int)(n); ++i)

int main()
{
#ifndef DEBUG
    vector<int> res;
    int a_tmp, b_tmp;
    char op_tmp;

    cin >> a_tmp >> op_tmp >> b_tmp;

    while (op_tmp != '?')
    {
        switch (op_tmp)
        {
        case '+':
            res.push_back(a_tmp + b_tmp);
            break;
        case '-':
            res.push_back(a_tmp - b_tmp);
            break;
        case '*':
            res.push_back(a_tmp * b_tmp);
            break;
        case '/':
            res.push_back(a_tmp / b_tmp);
            break;
        }
        cin >> a_tmp >> op_tmp >> b_tmp;
    }
    for (auto itr = res.begin(); itr != res.end(); ++itr)
    {
        cout << *itr << endl;
    }

#else
    char op_tmp;

    op_tmp = '?';
    cout << op_tmp;

#endif
}

