#include<cstring>
#include<cstdio>

int main(){
  char s[201], t[101], p[101];
  scanf("%s %s", s, p);
  strcpy(t, s); // tにsをコピー
  strcat(s, t); // sにtを連結

  if(strstr(s, p) == NULL) printf("No\n"); // 文字列sからpを検索
  else printf("Yes\n");

  return 0;
}

