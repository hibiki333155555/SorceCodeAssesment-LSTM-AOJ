#include <stdio.h>

void swap(int *x, int *y) {
	int temp;
	temp = *x;
	*x = *y;
	*y = temp;

	return;
}

int main() {
	int n = 3;
	int arr[ 100 ];

	for (int i = 0; i < n; i++) {
		scanf("%d", &arr[ i ]);
	}

	for (int i = 0; i < n; i++) {
		for (int j = i + 1; j < n; j++) {
			if (arr[ i ] > arr[ j ]) {
				swap(&arr[ i ], &arr[ j ]);
			}
		}
	}

	printf("%d ", arr[ 0 ]);
	printf("%d ", arr[ 1 ]);
	printf("%d ", arr[ 2 ]);
	printf("\n");

	return 0;
}
